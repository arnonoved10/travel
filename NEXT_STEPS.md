# NEXT_STEPS.md — קרא קודם כשפותחים Session חדש

**🔔 תזכורת פתוחה (2026-08-22):** המשתמש ביקש "לדלג כרגע" על מערכת השוואת-מחירים-אמיתית (מלון+טיסה עם עמלה) ולהמשיך אותה בעתיד — **הוא ביקש שנזכיר לו**. המצב הנוכחי: פאנל "השווה מחירים" עם קישורי-חיפוש חינמיים (בלי scraping, בלי API) כבר בנוי ועובד (ר' DECISIONS.md "סבב 13"). השלב הבא, כשהמשתמש ירצה להמשיך: הוא צריך להירשם בעצמו לתוכנית שותפים אמיתית (Booking.com Affiliate Partner API / Skyscanner Travel API / Amadeus for Developers — לא ניתן לבצע עבורו, דורש זהות אישית/עסקית וחתימה על הסכם), ואז לספק מפתח API/מזהה-שותף כדי לשדרג את הקישורים הקיימים למחירים חיים+עמלה אמיתית (שינוי קטן, לא בנייה מחדש).

**נכתב:** 2026-08-17, אחרי סיום שלב A (לפי "איפיון אפליקציה.docx" שהמשתמש סיפק).
**Commit אחרון בזמן הכתיבה:** ראה "Commit אחרון" ב-`PROJECT_STATE.md` (מתעדכן בכל commit — זה המקור האמין, לא המספר כאן).

## מקור האמת לתהליך העבודה: "איפיון אפליקציה.docx"

המשתמש סיפק קובץ Word (`איפיון אפליקציה.docx`, שמור ב-Git בשורש הפרויקט) שהוא מסמך אפיון מאוחד — כולל את כל 114 הדרישות **וגם** תהליך עבודה מחייב עם סדר שלבים מפורש:

| שלב | מטרה | סטטוס |
|---|---|---|
| **A** | השלמת כל 48 ה-PARTIAL וכל 8 ה-NOT IMPLEMENTED שאינם תלויים בשירות חיצוני | ✅ **הושלם** (10 סבבים, 2026-08-17) |
| **B** | Audit חדש עד שמרבית מה שניתן יהיה IMPLEMENTED מלא | ✅ **הושלם** (אותו Session) |
| **C** | שילוב חבילת ה-UI המאושרת לפי תמונת הייחוס | ⏳ ממתין לחומרי עיצוב מהמשתמש |
| **D** | החלת Design System על כל המסכים | ממתין ל-C |
| **E** | חיבור Supabase, Map, Weather, OCR ושירותים חיצוניים | 🟡 **התחיל חלקית** (Supabase DB בלבד, לבקשת המשתמש שקדמה לשלב A) |
| **F** | בדיקות End to End, Mobile, Offline, RTL, Performance, Security | סופי, אחרי E |

## מצב נוכחי בשורה אחת

**עודכן 2026-08-22 (סבב-2 של חבילת-הפיצ'רים המורחבת, אחרי סבב 14):** הספירה מול 114 הדרישות המקוריות **לא זזה** (עדיין 93/13/4/4) — סבב-2 (7 חלקים: תמונות-יום, `/stats`, אזהרת-תקציב, `Trip.tripType`+תבניות-אריזה, `/loyalty-programs`, שכפול-טיול-כולל-מסלול, הצבעות-מלווים) כולו תוספות מעבר לסקופ המקורי, לא מיפוי לדרישה קיימת. ר' FEATURE_AUDIT.md "עדכון 2026-08-22 (סבב-2)" ו-DECISIONS.md "סבב 15" לפרטים. **⚠️ עלה ל-10 migrations לא-מוחלים על ה-DB החי** — ר' PROJECT_STATE.md "מצב Database".

**עודכן 2026-08-22 (סבב 13):** 93/114 IMPLEMENTED, 13/114 PARTIAL, 4/114 NOT IMPLEMENTED, 4/114 BLOCKED — עלה מ-92 בזכות #88 Share Security (קישור-שיתוף מסלול ציבורי, token-based). באותו סבב נבנתה גם חבילת-פיצ'רים מורחבת מעבר ל-114 המקוריים (מלווים בטיול, חלוקת-הוצאות/סגירת-חשבונות, ממיר מטבע, כספומט קרוב, קצב-הוצאה, הצעות-מזג-אוויר) — ר' FEATURE_AUDIT.md "עדכון 2026-08-22 (סבב 13)" ו-DECISIONS.md "סבב 14" לפרטים.

**91/114 IMPLEMENTED, 13/114 PARTIAL, 6/114 NOT IMPLEMENTED, 4/114 BLOCKED BY EXTERNAL SERVICE (סבב 11, 2026-08-21).** המשתמש ביקש "את כל ה-18 הנותרים חלקי תשלים וותקן למצב מושלם" — כל אחד נבדק מחדש: 3 פערים אמיתיים נסגרו (#61 Refund-Pending — ישות+UI חדשים, #90 באג restore אמיתי ב-car_rental, #95 Payment.deletedAt נחשף), 2 תויגו מחדש כי היו עובדים בפועל (#82 PDF, #83 Excel), 1 שופר (#114), ו-12 אומתו כחסומים/מוגבלים-בכוונה כהלכה (Mapbox/OCR/DATA_SOURCE=prisma/עיצוב קפוא/Audit-Log-scope/דרוש-דפדפן-חי). **13 ה-PARTIAL שנשארו אינם "פירות נמוכים"** — לפני שמניחים שיש עוד "לתקן" קרא FEATURE_AUDIT.md ("עדכון 2026-08-21 (סבב 11)"). **⚠️ יש 2 migrations לא-מוחלים על ה-DB החי (Supabase לא הגיב, P1001) — הרץ `prisma migrate deploy` בהזדמנות הראשונה, ר' PROJECT_STATE.md "מצב Database".** Supabase **מחובר בפועל, RLS מופעל ומאומת** (DB חי, 38 טבלאות) אבל האפליקציה עדיין לא משתמשת בו (`DATA_SOURCE=mock`, נדחה בכוונה לבקשת המשתמש).

**חשוב:** 2026-08-18 המשתמש ביקש לתקן את כל 36 ה-PARTIAL כך שיעבדו במלואם. בוצע סבב מרוכז — 6 עברו ל-IMPLEMENTED, פיצ'ר אמיתי נבנה (קישור הצעת-מחיר-תחבורה↔הזמנה), עוד 4 שופרו. **30 ה-PARTIAL שנשארו נבדקו אחד-אחד ואינם "פירות נמוכים"** — הם חסימות שירות-חיצוני אמיתיות (Storage/Mapbox/DATA_SOURCE) או החלטות-עיצוב מכוונות ומתועדות (ר' DECISIONS.md). **אם המשתמש מבקש שוב "תקן את כל ה-PARTIAL" — אל תניח שיש עוד מה לתקן בלי קלט חיצוני; קרא קודם את הרשימה המפורטת ב-FEATURE_AUDIT.md ("מספרים מדויקים" + הטבלאות בתחתית) ותסביר לו את החלוקה במקום לצלול ישר לקוד.**

**מגבלה שיש לדעת:** שלב A לא כלל בדיקה חיה בדפדפן (P0-P3 הקודמים כן) — אין כלי דפדפן זמין בסביבת ה-Session שביצעה את שלב A. מומלץ אימות ידני (`npm run dev`, פורט 3101) של הפריטים שסומנו "עודכן (שלב A)" ב-FEATURE_AUDIT.md לפני שנחשבים ברמת ביטחון זהה לשאר האפליקציה.

## השלב הבא — המשתמש כבר ענה, אל תשאל שוב

**עודכן 2026-08-18:** השאלה "עיצוב או המשך חיבורים?" נשאלה בפועל באותו Session שכתב את הסעיף הזה, והמשתמש ענה: **"תמשיך כל דבר שאתה יכול לבצע לבד ללא משהו שאני צריך לבצע מהצד שלי"** — כלומר: המשך אוטונומי בכל מה שלא דורש ממנו מפתח/חשבון/קובץ. **אל תחזור לשאול את השאלה הזו ב-Session חדש** — התשובה כבר ניתנה ותקפה עד שהמשתמש עצמו יגיד אחרת.

בפועל, זה אומר: כל הבנייה שלא תלויה בקלט חיצוני כבר מוצתה בשלבים A+B (ר' "מצב נוכחי" למעלה). מה שנשאר מחולק לשניים:
- **שלב E, המשך:** תלוי במפתחות ש**רק המשתמש** יכול לספק — Mapbox Token, Claude API Key ל-OCR, ואישור-תזמון למעבר `DATA_SOURCE=prisma` (נדחה בכוונה, ר' סעיף 3 למטה).
- **שלב C (עיצוב):** תלוי בחומרי עיצוב שהמשתמש יעביר.

עד שאחד מהשניים מגיע, "המשך אוטונומי" אומר בעיקר: תחזוקה (typecheck/lint/test/build ירוקים, עדכון קבצי ההמשכיות), ותיקון אי-דיוקים שמתגלים תוך כדי — לא בניית פיצ'רים חדשים שאין להם עוד קרקע לעמוד עליה.

### שלב E — התקדמות (2026-08-17)

1. ✅ **RLS הופעל ואומת** — `prisma migrate deploy` (בוצע). ניסיון ראשון נכשל (באג אמיתי: `rls_policies.sql` הונח snake_case, ה-DB בפועל camelCase) — תוקן, אומת עם ניסיונות INSERT אנונימיים שנדחו נכון (`42501`) ל-3 טבלאות בדפוסי-policy שונים. ר' DECISIONS.md/PROJECT_STATE.md לפירוט המלא.
2. ✅ **הרשמה אמיתית נבדקה** — דרך `/auth/v1/signup` (אותו endpoint ש-`/register` קורא לו). הטריגר `handle_new_auth_user` יצר שורת `public.users` נכונה. משתמש הבדיקה נוקה בסוף.
3. 🔵 **מעבר בפועל מ-Mock ל-DB (`DATA_SOURCE=prisma`) — נדחה בכוונה לבקשת המשתמש, "בסוף".** נשאל אם לבצע עכשיו, המשתמש ביקש לדחות לשלב מאוחר יותר (לא הוגדר מתי בדיוק "בסוף" — כשמתקרבים לשם, לוודא מולו שזה עדיין הזמן הנכון, לא להניח). הסיבה שזו נקודה רגישה: ברגע שזה יקרה, האפליקציה דורשת session אמיתי (`getCurrentUser()` עובר לבדוק מול Supabase Auth) — אין יותר "פתח ותראה נתוני דמו מיד", המשתמש יצטרך להירשם+לאשר אימייל+להתחבר לפני שרואה כל מסך. עד שזה קורה, כל הפיתוח וההדגמה ממשיכים כרגיל מול Mock, בדיוק כמו היום.
4. **הבא בתור (לא תלוי בסעיף 3):** `NEXT_PUBLIC_MAPBOX_TOKEN` (חינמי, המשתמש צריך להירשם ב-account.mapbox.com), ולבסוף Claude API key ל-OCR (#43, 0% קוד — יידרש לבנות מאפס, לא רק לחבר). אלה לא תלויים במעבר ל-DATA_SOURCE=prisma — אפשר להתקדם איתם קודם אם המשתמש מספק את המפתחות.

**⚠️ לכל migration עתידי נגד ה-DB הזה: השתמש רק ב-`prisma migrate deploy`, לא `migrate dev`** (`migrate dev` תמיד מריץ Shadow Database, ומיגרציית ה-RLS מפנה ל-`auth.users` שלא קיים שם → נכשל תמיד).

### אפשרות 2: לעבור לשלב C (עיצוב)

המשתמש יעביר "חבילת UI סופית" (design package). כשזה קורה:
- **Component Library** (14 רכיבים: Modal/Drawer/Tooltip/Toast/FilterChip/PageHeader/MapCard/WalletCard/ExpenseCard/BookingCard) נדחה בכוונה עד עכשיו בדיוק בשביל זה — כנראה אחד הדברים הראשונים שכדאי להעלות מחדש.
- **Localization מלא** (#91, i18n) נדחה מאותה סיבה — שינוי מבנה קבצים נרחב שעלול להתנגש עם שינויי layout.
- קרא את `DECISIONS.md` לפני שנוגעים ב-Design System v2 הקיים (מתועד שם עם נימוקים לכל בחירה — טוקנים/Glass/Ambient Glow).

**אם המשתמש פותח Session חדש ואומר "תמשיך" בלי הקשר נוסף — אל תשאל שוב "עיצוב או המשך חיבורים?" (כבר נענה, ר' למעלה). בדוק במקום זה אם הוא צירף בינתיים חומרי עיצוב או מפתחות API; אם לא, הרץ verification (typecheck/lint/test/build) ודווח שהמצב יציב וממתין לקלט חיצוני.**

## קבצים לקרוא, בסדר הזה

1. **הקובץ הזה (`NEXT_STEPS.md`)** — כבר כאן.
2. **`איפיון אפליקציה.docx`** — מסמך האפיון המאוחד (בינארי — לחלץ טקסט: `unzip -p "איפיון אפליקציה.docx" word/document.xml` ואז לנקות תגי XML, או לפתוח ב-Word/LibreOffice). מגדיר את כל 114 הדרישות + תהליך העבודה A-F.
3. **`FEATURE_AUDIT.md`** — הסטטוס המדויק של כל 114 הדרישות, שורה-אחר-שורה. תחתית הקובץ (סעיף "Audit סופי — שלב A") מסכם הכול.
4. **`PROJECT_STATE.md`** — היסטוריית הבנייה המלאה, כולל "מצב Supabase"/"מצב Database" עם כל הפרטים על החיבור החי.
5. **`IMPLEMENTATION_GAPS.md`** — רשימת מה שנשאר (עכשיו: DEFERRED TO DESIGN + BLOCKED BY EXTERNAL SERVICE בלבד — שלב A/B סגורים).
6. **`DECISIONS.md`** — למה נבחרו הפתרונות שנבחרו.
7. **`PROJECT_REQUIREMENTS.md`** — המסמך המקורי (156 סעיפים, מיפוי היסטורי).

## מוסכמות Git (ללא שינוי)

ריפו מקומי בלבד, **אין Remote, לא בוצע Push מעולם**. כל batch עבודה: commit אחד לתוכן (הודעה בעברית מפורטת), ואז commit נפרד "תיקון הפניית hash ל-commit האחרון ב-PROJECT_STATE.md". לעולם לא `git add -A` — קבצים מפורשים בלבד. **תיקיית "אפליקציית ניהול מוניות" ליד `טיולים` היא פרויקט אחר לגמרי — לעולם לא לגעת בה.**

## הערת אבטחה חשובה

ה-Secret Key של Supabase וסיסמת ה-DB הודבקו בצ'אט ע"י המשתמש (לא ביוזמת Claude). הם נשמרו רק בקבצי `.env` המוחרגים מ-Git — לא הוצגו/הודפסו שוב בשום תגובה אחרי שנשמרו. אם המשתמש מודאג מכך שההיסטוריה של הצ'אט מכילה אותם, אפשר לסובב (Rotate) את ה-Secret Key ולאפס את סיסמת ה-DB בדשבורד של Supabase בכל שלב — יש לעדכן את שני קבצי ה-`.env` (`apps/web/.env.local`, `packages/db/.env`) בהתאם אם זה קורה.
