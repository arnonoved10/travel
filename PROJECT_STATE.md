# PROJECT_STATE.md — מקור האמת למצב הפרויקט

**עודכן לאחרונה:** 2026-08-21 (סבב 11 — כל 18 ה-PARTIAL הנותרים נבדקו מחדש, 5 עברו ל-IMPLEMENTED — ראה `FEATURE_AUDIT.md` סעיף "עדכון 2026-08-21 (סבב 11)")
**קרא גם:** **`NEXT_STEPS.md` (קרא את זה ראשון — לאן ממשיכים מכאן)**, `PROJECT_REQUIREMENTS.md` (מה האפליקציה אמורה לעשות), `DECISIONS.md` (למה הוחלט מה שהוחלט), `01_architecture_v2.md` (ארכיטקטורת הנתונים המלאה), `packages/db/prisma/schema.prisma` (סכימה בפועל).

**חשוב לכל Session/Compact חדש:** קרא את `NEXT_STEPS.md` קודם, ואז את שאר הקבצים למעלה. בדוק `git log --oneline -5` ו-`git status`, ורק אז המשך. אל תסתמך על זיכרון מה-Context הקודם.

---

## מצב סופי — 2026-08-21 (סבב 11: כל 18 ה-PARTIAL נבדקו, 91/114 IMPLEMENTED)

המשתמש ביקש במפורש "את כל ה-18 הנותרים חלקי תשלים וותקן למצב מושלם". כל אחד מ-18 ה-PARTIAL נבדק מחדש מול קוד בפועל: **3 פערים אמיתיים נסגרו** (Refund-Pending/#61 — ישות+UI+Repository חדשים; באג restore אמיתי ב-#90 — `car_rental` היה חסר מ-`idMapByEntityType`; #95 Soft Delete — `Payment.deletedAt` היה קיים בסכימה אבל לא חשוף בכלל בשכבות העליונות), **2 תויגו מחדש** (#82 PDF, #83 Excel — שניהם עובדים מקצה-לקצה, התיוג הקודם היה לא מדויק), **1 שופר בלי לשנות תיוג** (#114 — הורחב ל-2 מסכים נוספים כולל תיקון UX אמיתי במסך חירום), ו-**12 אומתו כחסומים/מוגבלים-בכוונה כהלכה** (Mapbox/OCR/DATA_SOURCE=prisma/עיצוב קפוא/היקף Audit Log/דרוש דפדפן חי לאימות) — ר' `FEATURE_AUDIT.md` "עדכון 2026-08-21 (סבב 11)" לפירוט המלא כולל אילו קבצים השתנו.

**91/114 IMPLEMENTED, 13/114 PARTIAL, 6/114 NOT IMPLEMENTED, 4/114 BLOCKED BY EXTERNAL SERVICE.** typecheck+lint+test(326)+build ירוקים.

---

## מצב סופי — 2026-08-18 (שלבים A+B הושלמו, E בעבודה, סבב תיקון PARTIAL הושלם) [ארכיון]

המשתמש סיפק קובץ אפיון מאוחד (`איפיון אפליקציה.docx`, שמור ב-Git) שמגדיר תהליך עבודה מחייב וסדר שלבים **A–F**: A=השלמת פנימי, B=אודיט חוזר, C=שילוב חבילת עיצוב, D=עיצוב על כל המסכים, E=חיבור שירותים חיצוניים, F=בדיקות סופיות. **שלבים A+B הושלמו במלואם.** בהמשך, לבקשת המשתמש, **שלב E התקדם משמעותית**: RLS הופעל בפועל על ה-DB החי ואומת. לאחר מכן המשתמש ביקש לתקן את **כל 36** הפריטים ש-FEATURE_AUDIT.md סימן PARTIAL כך שיעבדו במלואם — בוצע סבב מרוכז (2026-08-18): 6 עברו ל-IMPLEMENTED, פיצ'ר אמיתי חדש נבנה (קישור הצעת-מחיר-תחבורה להזמנה), ועוד 4 שופרו מהותית. ר' `FEATURE_AUDIT.md` ("עדכון 2026-08-18") לפירוט המלא כולל מה נבדק ונשאר ללא שינוי בכוונה.

**86/114 IMPLEMENTED (+32 מהבסיס), 18/114 PARTIAL (-30), 6/114 NOT IMPLEMENTED (-2), 4/114 BLOCKED BY EXTERNAL SERVICE (ללא שינוי מספרי, אך #101 Supabase התקדם משמעותית בפועל — RLS פעיל ומאומת, ר' "מצב Supabase" למטה).** #91 Localization ו-Component Library נשארים נדחים בכוונה לשלב העיצוב (C/D). לאחר שאלת המשתמש "למה 30 לא עובדות במלואן" בוצעה בדיקת-המשך שמצאה עוד פער אמיתי אחד (#18 Flights, ואח"כ #33 Expenses). ב-2026-08-21 המשתמש ביקש להשלים את #4 Trip Days (TripDay כישות אמיתית עם הערות-יום), נתן רשימה מפורשת של 11 פריטים נוספים (9 עברו PARTIAL→IMPLEMENTED, סבב 9), ואז הצביע שוב על #7 Places — שדרש שינוי סכימה אמיתי (Document.tripId nullable + תיקון RLS, סבב 10). **⚠️ שני migrations חדשים מסבב 10 טרם הוחלו על ה-DB החי** — Supabase לא הגיב (P1001), כנראה נכנס למצב שינה. חובה `prisma migrate deploy` לפני מעבר ל-DATA_SOURCE=prisma. ר' FEATURE_AUDIT.md "סבב 9"/"סבב 10" לפירוט המלא.

**מגבלה מתועדת בכנות:** העבודה מאז תחילת שלב A לא כללה בדיקה חיה בדפדפן (P0-P3 הקודמים כן כללו) — אין כלי דפדפן זמין בסביבת ה-Session הנוכחית. אימות מבוסס על typecheck+lint+test(322)+build ירוקים אחרי כל סבב, ובשלב E גם על בדיקות API ישירות מול ה-DB החי (curl). מומלץ למשתמש לבדוק ידנית בדפדפן את הפריטים שסומנו "עודכן" ב-FEATURE_AUDIT.md.

**מעבר `DATA_SOURCE=prisma` נדחה בכוונה לבקשת המשתמש ("בסוף")** — ראה "מצב Supabase" ו-`NEXT_STEPS.md`.

**השלב הבא:** 30 ה-PARTIAL שנותרו מחולקים כך: רובם חסימות אמיתיות (תמונות/גלריות התלויות ב-Storage, Explore-This-Area/Marker-Clustering התלויים ב-Mapbox, בדיקת-שני-משתמשים התלויה ב-DATA_SOURCE=prisma) או החלטות-עיצוב מכוונות ומתועדות (PDF/Excel דרך הדפדפן, Tourist Tax/Tip כ-Expense, Audit Log מוגבל בהיקף, Day Summary/End Trip Summary ללא עמוד נפרד). אין עוד "פירות נמוכים" לקטוף בלי קלט חיצוני או בלי לבטל החלטות עיצוב קודמות. לבחור מתי להשלים את יתר שלב E (Mapbox Token, Claude API ל-OCR, ולבסוף DATA_SOURCE) או לעבור לשלב C (שילוב חבילת עיצוב, ממתין למשתמש). **ראה `NEXT_STEPS.md` לפירוט המלא.**

typecheck/lint/test(322 בדיקות)/build — כולם ירוקים נכון לתאריך זה.

---

## מטרת האפליקציה
מערכת ניהול טיולים אישית (בעברית, RTL) — מתכננת ומנהלת טיול מקצה לקצה: תכנון עתידי → הזמנות (מלונות/טיסות/תחבורה) → תשלומים/ארנק רב-מטבעי → מסמכים/מפות/מסלולים → שימוש בזמן אמת במהלך הטיול (מסך "היום שלי"/"עכשיו") → סיכום ודוחות אחרי החזרה. כולל בעתיד: מזג אוויר לפי מיקום+זמן אמיתי, המלצות, אינטגרציות חיצוניות (Booking.com וכו').

## Tech Stack בפועל
- Next.js 16.3.1 (App Router, Turbopack), React 19.2.8, TypeScript 7.0.2 (strict)
- Prisma 6.19.3 (schema מוכן, **DB חי לא מחובר עדיין**)
- Zod 4.4.3 (ולידציה בכל שכבה)
- Supabase Auth+DB+Storage — **קוד מוכן, לא מחובר** (ראה "מצב Supabase" למטה)
- Leaflet 1.9 + react-leaflet 5 (מפה, OpenStreetMap tiles — חינמי, בלי API key)
- Vitest 3 (בדיקות), ESLint 9 (flat config), npm workspaces (monorepo)
- Git מקומי בלבד, בלי remote

## מבנה הפרויקט
```
טיולים/
├── PROJECT_STATE.md, PROJECT_REQUIREMENTS.md, DECISIONS.md   ← קבצי המשכיות (הקובץ הזה)
├── 01_architecture_v2.md          ← ארכיטקטורת נתונים מלאה, מעודכנת
├── docs/PHASE_0_REPORT.md
├── .claude/launch.json            ← מגדיר dev server בפורט 3101
├── apps/web/                      ← Next.js. app/(app)/ = מסכים מאומתים (nav+banner+auth gate)
│   ├── lib/auth/get-current-user.ts   ← demo user / Supabase session, לפי DATA_SOURCE
│   ├── lib/supabase/{client,server,admin,env}.ts
│   └── app/(app)/{dashboard,today,trips,places}/...
├── packages/db/                   ← Prisma schema (38 טבלאות) + rls_policies.sql (טרם הופעל)
├── packages/shared-types/         ← Zod schemas + TS types, משותף client/server
└── packages/data-layer/           ← Repository pattern: Mock (in-memory) ⇄ Prisma (pending), נבחר לפי DATA_SOURCE env
```

## שלב נוכחי
**שלבים 2+3.5 הושלמו, שלב 4 (Documents) וגם שלב 5 (Offline) התחילו בהיקף מוגבל בכוונה** — עדיין בפיתוח UI מול Mock Data Layer. Supabase חי נדחה ביוזמת המשתמש (2026-08-15), לא חוסם המשך פיתוח. ראה DECISIONS.md.

## מה כבר הושלם (ונבדק בדפדפן בפועל, לא רק בקוד)
1. **תשתית (שלב 0):** מונו-רפו, Next.js, Prisma schema, ESLint/Vitest/TypeScript strict — כולם build/test/lint ירוקים.
2. **Data Layer abstraction:** `packages/data-layer` — Repository לכל ישות (Trip/Place/Booking/Finance/TripPlace), כל אחד עם מימוש Mock (in-memory, seeded, מסומן `[דמו]`) ומימוש Prisma מקביל (קוד אמיתי, לא נבדק live, מסומן `PENDING_INTEGRATION`). מוחלף ע"י `DATA_SOURCE=prisma` בלי לשנות UI.
3. **App Shell:** ניווט רספונסיבי (בר תחתון מובייל / סיידבר דסקטופ), באנר "מצב פיתוח" גלוי, Dashboard עם ספירות אמיתיות.
4. **Trips:** רשימה, יצירה, עריכה (כולל שינוי סטטוס), הארכה/קיצור תאריכים (נבדק: 5→10 ימים בלוח), Soft Delete + שחזור (נבדק מלא), Validation (תאריך סיום לפני התחלה נדחה), לוח ימים מחושב חי.
5. **Places:** ספרייה גלובלית, סינון קטגוריה, יצירה, Soft Delete.
6. **TripPlace:** קישור מקום מהספרייה לטיול עם סטטוס (נבדק).
7. **Bookings:** מלונות/טיסות/תחבורה — רשימה+יצירה לכל סוג, בתוך מסך טיול.
8. **כספים:** Wallet (יתרה לפי מטבע, טעינה), Expenses (יצירה+קטגוריה), Payments — **תשלום מזומן מפחית אוטומטית מהארנק** (נבדק חי: 19850→19750 THB בדיוק), תשלום אשראי לא נוגע בארנק. **Currency Exchange** — המרה בין שני מטבעות מעדכנת שני ארנקים אוטומטית (נבדק חי: 19850→23050 THB, ארנק USD חדש נוצר אוטומטית).
9. **"היום שלי":** מסך חוצה-טיולים, מוצא טיול פעיל לפי תאריך אמיתי, מציג מלון/טיסות/הסעות/הוצאות של היום; מצב "אין טיול פעיל" נבדק בנפרד.
10. **Tips:** שדות ייעודיים על Expense (מקבל + קטגוריית טיפ), טופס מציג אותם רק כש-category=tip, דוח טיפים (סה"כ + פילוח קטגוריה) בתוך מסך הטיול — נבדק חי.
11. **Contacts:** מרכז אנשי קשר גלובלי (לא לפי טיול), רשימה+יצירה+Soft Delete, קטגוריות (מלון/נהג/חברת מוניות/ביטוח/השכרה/תעופה/מעבורת/מדריך/סוכן/אטרקציה/אחר) — נבדק חי.
12. **Insurance:** כסוג Booking רביעי (כמו מלון/טיסה/תחבורה) — חברה, סוג פוליסה, תוקף, מספר פוליסה, טלפון חירום — נבדק חי.
13. **Refund:** מקושר להוצאה מקורית, מזכה אוטומטית את הארנק במטבע המתאים (בקשת המשתמש — החזרי מס וכו') — נבדק חי.
14. **קטגוריות הוצאה דינמיות:** Expense.category הפך מ-enum ל-String חופשי, עם datalist של 14 הצעות ברירת מחדל — אפשר להוסיף קטגוריה חדשה בלי migration. נבדק חי עם קטגוריה חדשה לגמרי ("צלילה").
15. **Place — כתובת/lat/lng + ניווט:** טופס יצירת מקום כולל כתובת, lat, lng (אופציונלי, אבל שניהם ביחד או כלום — נאכף ב-Zod). כפתורי "🧭 Google Maps" / "🚗 Waze" (deep-link, בלי API key) מופיעים בספריית המקומות ובמקומות המקושרים לטיול — מעדיפים קואורדינטות, נופלים חזרה לכתובת אם אין. נבדק חי: גם מקום עם קואורדינטות (13.7563,100.5018) וגם מקום עם כתובת בלבד ("Khao San Rd, Bangkok").
16. **מסך מפה (`/map`):** Leaflet + OpenStreetMap (חינמי, ללא API key) — מציג את כל המקומות עם lat/lng כ-markers, popup עם שם/קטגוריה/כתובת + אותם כפתורי ניווט, סינון קטגוריה (chips כמו ב-/places), מקומות בלי קואורדינטות מוצגים ברשימה נפרדת מתחת למפה. נוסף לניווט הראשי. נבדק חי: markers ממוקמים נכון, popup נפתח בלחיצה עם קישורים עובדים.
17. **מסאז'ים/אוכל/פירות/קניות/אטרקציות — שדות ייעודיים:** טופס הוצאה מציג בחירת מקום מהטיול, דירוג אישי (1-5 כוכבים), ולקניות גם שם פריט+כמות, כשהקטגוריה היא אחת מ-5 אלו. Expense.placeId/personalRating היו קיימים בסכימה אבל נזרקו בשקט ביציאה מה-Mock — תוקן בכל השכבות. תמונה נדחית (תלוי Storage, כמו Documents). נבדק חי: הוצאת קניות עם מקום מקושר, 5 כוכבים, "מזכרות לבית (x3)".
18. **מסך "עכשיו" (`/now`):** נפרד מ"היום שלי" — מציג איפה ישן/צ'ק-אין-אאוט היום, האירוע הבא (טיסה/הסעה) עם ספירה לאחור חיה ("בעוד X שעות ודקות"), שאר האירועים להיום, הוצאתי היום, יתרות ארנק. שני סעיפים מהמפרט המקורי מסומנים בפירוש כ"בקרוב" (לא מזויפים): מקומות לידי (דורש הרשאת מיקום) ובדיקת חוסרים באזור (שלב 2). נבדק חי: מצב "אין טיול פעיל", ואחרי הזזת תאריכי טיול הדמו זמנית — האירוע הבא + ספירה לאחור + רשימת שאר היום.
19. **דוח טיול בסיסי (`/trips/[tripId]/report`):** עלות כוללת + ממוצע ליום (לפי מטבע — הטיול מרובה מטבעות), פילוח קטגוריות, מזומן מול שאר אמצעי התשלום, טיפים, יתרות ארנק שנשארו, מקומות לפי סטטוס. קישור "📊 דוח טיול" בראש מסך הטיול. נבדק חי מול נתוני הדמו (450 THB סה"כ, 30 THB/יום, תואם בדיוק).
20. **Planned Activity (תכנון עתידי, שלב 2 — התחלה):** Repository מלא (Mock+Prisma+7 בדיקות) + סעיף "תכנון עתידי" במסך הטיול — רשימה, יצירה (שם/קטגוריה/תאריך-שעה מתוכננים/משך/מחיר משוער/מטבע/הערות), שינוי סטטוס inline (dropdown, כל 10 הערכים ב-LifecycleStatus), הסרה. נבדק חי: יצירה + שינוי סטטוס מ-want_to_book ל-booked ואישור ששרד hard reload. **המרה בפועל ל-Booking (בחירת תת-סוג מלון/טיסה/תחבורה/ביטוח) — Not Started**, נדחתה בכוונה כמשימה נפרדת ומורכבת יותר.
21. **מסלול יומי (Route):** Repository מלא (Mock+Prisma+8 בדיקות) + עמוד `/trips/[tripId]/days/[date]` (מקושר מכל תא ביומן הטיול) — רשימת עצירות מסודרת עם כפתורי ⬆️⬇️ לשינוי סדר, הסרה, כפתורי ניווט לכל עצירה. ה-API מזוהה לפי (tripId, date) ישירות, לא tripDayId — ראה DECISIONS.md. נבדק חי: הוספת 2 עצירות, שינוי סדר, ואישור ששרד hard reload.
22. **מנוע בדיקת חוסרים (Gap Detection) — משלים את שלב 2:** פונקציה טהורה (`apps/web/lib/gap-detection.ts`, 12 בדיקות) שרצה על נתונים שכבר הוזנו בפועל בלבד (לא ממציאה מידע) — 6 בדיקות: לילות בלי מלון, חפיפת מלונות, תכניות "צריך להזמין", תכניות עם תאריך שעבר בלי סטטוס סופי, אין ביטוח בכלל, טיסה בלי הסעה רשומה באותו יום. מוצג כבאנר "⚠️ בדיקת חוסרים" בראש מסך הטיול (מוסתר לגמרי כשאין חוסרים). נבדק חי מול נתוני הדמו: 9 חוסרים אמיתיים נמצאו, ואחרי הוספת ביטוח הספירה ירדה ל-8 עם החוסר הספציפי נעלם. **שלב 2 (01_architecture_v2.md) הושלם במלואו עם זה.**
23. **Documents (קבלות על הוצאות, שלב 4 — התחלה):** Repository מלא (Mock+Prisma+6 בדיקות) עם קישור פולימורפי (entityType/entityId, כל הסוגים בסכימה). ב-Mock, קובץ שהועלה (עד 3MB) הופך ל-`data:` URI אמיתי בזיכרון — לא כתובת מזויפת, אין Storage ענן מחובר (ראה DECISIONS.md). UI: העלאת קבלה על הוצאה + תצוגת 📎 בשורת ההוצאה + הסרה. OCR לא מופעל בפועל (`ocrStatus` נשאר "pending" בכנות — דורש Claude API key). נבדק חי: העלאת PNG אמיתי, קישור עובד, הסרה. **סוגי ישות אחרים (Booking/Insurance/וכו') — Not Started ב-UI** (נתמכים במודל).
24. **Offline בסיסי (שלב 5 — התחלה):** Service Worker (cache-first לנכסי build, נפילה לעותק אחרון של דף כשאין רשת), manifest.json (PWA installable), באנר "לא מקוון" (`useSyncExternalStore` על navigator.onLine) — כולם נבדקו: manifest+sw.js מוגשים נכון (200, content-type תקין), מעבר offline↔online של הבאנר עובד נכון בדפדפן. **רישום ה-Service Worker לא אומת בפועל** בכלי הדפדפן הממוחשג של הסביבה הזו (נכשל עם שגיאה גנרית) — ייתכן מגבלת סביבה, לא באג מאומת, ראה DECISIONS.md. **סנכרון offline דו-כיווני אמיתי (Dexie + תור סנכרון) — Not Started בכוונה**, דורש שינוי ארכיטקטוני (כל האפליקציה על SSR+Server Actions) שלא בוצע בלי אישור מפורש — ברשימת נושאים לסוף.
25. **Audit Log (יומן שינויים):** Repository גנרי (Mock+Prisma+5 בדיקות, entityType/entityId פולימורפי). מחובר בפועל לשני מקומות: עדכון שדות Trip (דיף מלא מול המצב הקודם) ושינוי סטטוס PlannedActivity. סעיף "יומן שינויים" במסך הטיול, ממוזג ומסודר מהחדש לישן. Coverage חלקי בכוונה — לא כל Repository באפליקציה (ראה DECISIONS.md). נבדק חי: עריכת שם טיול תפסה גם שינוי לא מכוון בשדה אזור זמן (side effect אמיתי, לא מבוים), ושינוי סטטוס תכנון נוסף כרשומה שלישית ממוינת נכון.
26. **Soft Delete ל-Expense + 4 סוגי Booking:** `deletedAt`+`softDelete` נוספו ל-HotelStay/Flight/TransportBooking/Insurance ול-Expense (Mock+Prisma, 12 בדיקות), כפתור "הסר" בכל שורה במסך הטיול. **Payment הושאר בכוונה בלי Soft Delete** — מחיקתו בלי לתקן את יתרת הארנק שכבר הופחתה הייתה יוצרת חוסר עקביות, זו החלטת עיצוב נפרדת (ראה DECISIONS.md). מחיקת Expense גם היא לא מתקנת תשלומים שכבר נרשמו עליה — מתועד בהודעת האישור. נבדק חי: מחיקת הטיסה וההוצאה של הדמו, שתיהן נעלמו מהרשימה והספירה ירדה ל-0.
27. **היסטוריית תנועות ארנק + Deposit (פיקדון):** כל פעולה שמשפיעה על יתרת ארנק (טעינה/תשלום מזומן/המרה/החזר/פיקדון) רושמת עכשיו שורת `WalletTransaction` (Mock+Prisma, 6 בדיקות חדשות — סה"כ 88 בדיקות ב-data-layer). סעיף "היסטוריית תנועות ארנק" במסך הטיול מציג את כולן ממוינות מהחדש לישן. **Deposit** — ישות חדשה לפיקדון ששולם (סכום/מטבע/למי שולם/סיבה/תאריך החזר צפוי), מפחית מהארנק ברגע התשלום; כפתור "סמן כהוחזר" מזכה את הארנק בסכום שהוחזר בפועל (יכול להיות שונה מהסכום המקורי). נבדק חי: פיקדון 500 THB הפחית את הארנק בדיוק ל-19350, סימון "הוחזר" עם 450 THB החזיר את הארנק ל-19800, ושתי התנועות הופיעו נכון בהיסטוריה (מהחדש לישן).
28. **PaymentCard (אמצעי תשלום/כרטיסי אשראי):** ישות גלובלית חדשה (כמו Contacts, לא לפי טיול) — שם כרטיס + מטבע ברירת מחדל אופציונלי (Mock+Prisma, 3 בדיקות חדשות — סה"כ 91 בדיקות ב-data-layer). עמוד `/payment-cards` (רשימה+יצירה), מקושר מעמוד אנשי הקשר. `Payment.cardId` היה קיים בסכימה בלי דרך להזין אותו — טופס התשלום מציג עכשיו בחירת כרטיס כש-`paymentMethod=credit_card` (עם קישור "הוסף כרטיס" אם אין עדיין אף אחד). נבדק חי: יצירת כרטיס חדש, בחירתו בתשלום 100 THB באשראי, ואישור שסכום ה"שולם" בהוצאה עודכן נכון (150 מזומן דמו + 100 חדש = 250).
29. **StatusHistory + דוח "תכנון מול ביצוע":** StatusHistory היה קיים בסכימת Prisma (טבלה פולימורפית entityType/entityId/oldStatus/newStatus) בלי שום מימוש. Repository חדש (Mock+Prisma, 3 בדיקות — סה"כ 94 בדיקות ב-data-layer), מחובר לשינוי סטטוס PlannedActivity **במקביל** ל-AuditLog הקיים (StatusHistory ייעודי למעברי סטטוס, AuditLog לדיף שדות כללי — שני מנגנונים שונים, לא כפילות). כרטיס חדש בעמוד דוח הטיול מציג לכל פריט תכנון עתידי את שרשרת מעברי הסטטוס בפועל עם זמנים, לעומת הסטטוס הנוכחי. נבדק חי: שינוי סטטוס פעמיים (want_to_book→planned→booked) הופיע נכון בדוח כשרשרת כרונולוגית עם timestamps אמיתיים.
30. **שעות פתיחה + "פתוח עכשיו" ל-Place:** `Place.openingHours` (Json?) היה קיים בסכימת Prisma בלי צורה מובנית ב-shared-types/UI. נוספה סכימת Zod (שעת פתיחה/סגירה לכל יום בשבוע, ולידציית HH:MM), חובר ל-Mock+Prisma Place repositories, ונבנתה פונקציה טהורה `isOpenNow()` (5 בדיקות — סה"כ 99 בדיקות ב-apps/web+data-layer) שתומכת בטווח רגיל, יום סגור מפורש, ושעות שחוצות חצות — מחזירה `null` (לא `false`) כשאין נתון בכלל, כדי שהבאדג' לעולם לא ימציא סטטוס למקום בלי שעות מוגדרות. UI: עורך שעות בטופס יצירת מקום (checkbox+שעות לכל יום), ותג "🟢 פתוח עכשיו"/"🔴 סגור עכשיו" בעמוד המקומות ובמקומות המקושרים לטיול. נבדק חי: מסעדת הדמו (ב'-ש' 10:00–22:00, סגורה בראשון) הראתה נכון "סגור" ביום ראשון; מקום בדיקה עם שעות ראשון 00:00–23:59 הראה נכון "פתוח".
31. **סינון טיולים לפי טווח תאריכים:** עמוד `/trips` תומך ב-`?from=&to=` (טופס GET רגיל, בלי JS צד לקוח) — מציג רק טיולים שחופפים לטווח (startDate/endDate). "X טיולים (מתוך Y)" כשיש סינון פעיל + קישור "נקה סינון". נבדק חי: סינון למרץ 2026 בודד נכון את טיול פראג מתוך 2 טיולי דמו; טווח מחוץ לתחום הראה נכון מצב ריק.
32. **תחזית שעתית/יומית במסך "היום שלי":** `OpenMeteoWeatherProvider.getHourlyForecast()/getDailyForecast()` היו ממומשים במלואם אבל ה-UI הציג רק תנאים נוכחיים. כרטיס "מזג אוויר" ב-`/today` מציג עכשיו גם 8 השעות הקרובות (אייקון+טמפ', גלילה אופקית) וגם 5 הימים הקרובים (מצב+מקסימום/מינימום) — נתונים אמיתיים בלבד, נטענים במקביל לתנאים הנוכחיים. נבדק חי: הזזת תאריכי טיול הדמו זמנית לכלול היום + קישור מקום עם קואורדינטות — תחזית אמיתית לבנגקוק (00:00–07:00, 26–27°C) ו-5 ימים עם תנאים אמיתיים הופיעו נכון.
33. **השכרת רכב/אופנוע (CarRental) — סוג Booking חמישי:** `bookingType=car_rental` היה קיים כערך enum בלי טבלה ייעודית (בניגוד למלון/טיסה/תחבורה/ביטוח). נוסף `CarRental` ל-schema.prisma (סוג רכב/חברה/דגם/מספר רישוי/איסוף+החזרה עם אזור זמן/דרישות נהג/ביטוח כלול/פיקדון) ו-enum חדש `RentalVehicleType` (רכב/אופנוע/קטנוע/אופניים/אחר — נפרד מ-VehicleType הקיים, ששייך לקטגוריות נפח נוסעים למונית ולא רלוונטי כאן). BookingRepository הורחב (Mock+Prisma+4 בדיקות — סה"כ 101 בדיקות ב-data-layer), UI חדש (טופס+רשימה+מחיקה) בדיוק לפי הדפוס של Insurance. נבדק חי: השכרת אופנוע (Honda Click 125, פיקדון 3000 THB, ביטוח כלול) נוצרה ומוצגת נכון.
34. **המרת Planned Activity ל-Booking אמיתי:** התגלה ותוקן פער אדריכלי קיים — `PlannedActivity.bookingId` הוא FK אמיתי ל-`Booking.id` בסכימה, אבל 5 הטיפוסים המשוטחים (HotelStay/Flight/TransportBooking/Insurance/CarRental) חשפו רק את id של תת-הטבלה, אף פעם לא את Booking.id האמיתי — נוסף שדה `bookingId` לכל אחד מהם (Mock+Prisma). נוסף `PlannedActivityRepository.getById()/linkToBooking()`. 5 פעולות היצירה הקיימות (`create*BookingAction`) מקבלות עכשיו `plannedActivityId` אופציונלי — כשמוגדר, מקשרות את ההזמנה החדשה לתכנון ומעבירות סטטוס ל-booked, בלי למחוק/לשכפל את התכנון. עמוד חדש `/trips/[tripId]/planned-activities/[id]/convert` — בחירת תת-סוג + טופס ממולא מראש משם/תאריך/מחיר התכנון. נבדק חי מקצה לקצה: המרת "[דמו] סיור באי פי פי" להסעת מעבורת (Rassada Pier → Phi Phi Island) — הטופס מולא מראש, ניתוב חזרה לטיול עבד, ההזמנה החדשה הופיעה ברשימת התחבורה, התכנון קיבל תג "✅ מקושר להזמנה" וסטטוס booked, וכניסה חוזרת לעמוד ההמרה הציגה נכון "כבר הומר".
35. **"מקומות קרובים אליי" במסך עכשיו (Geolocation):** כרטיס "בקרוב" ב-`/now` אמר "דורש הרשאת מיקום, עדיין לא ממומש". מומש עם Geolocation API של הדפדפן (חינמי, בלי מפתח/שירות חיצוני — כמו Leaflet+OSM ו-Open-Meteo) ופונקציה טהורה `haversineDistanceKm()` (5 בדיקות — סה"כ apps/web 36 בדיקות). רכיב client חדש `NearbyPlaces` מציג את המקומות המקושרים לטיול (עם קואורדינטות אמיתיות בלבד) בטווח 30 ק"מ, ממוינים מהקרוב לרחוק, עם סטטוס וכפתורי ניווט. בלי הרשאה/בלי מקומות עם קואורדינטות — הודעה כנה, לא בדיה. נבדק חי: בדפדפן הבדיקה (שדוחה הרשאת מיקום כברירת מחדל) הוצגה נכון הודעת "אין הרשאה"; עם מיקום מדומה בקואורדינטות המלון בדיוק, המלון הוצג ב-"0 מ'" והמסעדה המקושרת ב-"3.2 ק"מ", ממוינים נכון.
36. **דוח טיול — סינון תאריכים + גרפים:** `/trips/[tripId]/report` תומך עכשיו ב-`?from=&to=` (מסנן את כל ההוצאות לפני שכל המדדים מחושבים מחדש — סה"כ/ממוצע ליום/פילוח קטגוריה/אמצעי תשלום/טיפים). רכיב `BarChart` חדש (CSS/flex גרידא, בלי ספריית תרשימים) מציג גרף עמודות אופקי לפילוח קטגוריות ולהוצאות לפי יום, גרף נפרד לכל מטבע (אין המרה/ניחוש שער חליפין). נכון ל-RTL אוטומטית (הפס הממולא הוא flex item יחיד שמכבד dir="rtl"). נבדק חי: הוצאה שנייה בקטגוריה שונה הראתה שני עמודות פרופורציונליות; סינון לטווח ריק הראה נכון מצב ריק בכל הכרטיסים עם מונה "X מתוך Y".
37. **קנבן גרירה ל-Planned Activity:** "תכנון עתידי" הוצג רק כרשימה שטוחה עם dropdown לסטטוס — סומן במפורש כפער בארכיטקטורה. נוסף רכיב client `PlannedActivitiesView` עם טוגל בין "רשימה" (הקיים) ל"קנבן" (עמודה לכל LifecycleStatus, HTML5 drag-and-drop native בלי ספריה חיצונית) — גרירת כרטיס בין עמודות קוראת לאותה `updatePlannedActivityStatusAction` שמשמשת את ה-dropdown. State מקומי לעדכון אופטימי, מתואם מול ה-prop מהשרת ב-render (לא ב-useEffect, כדי לעמוד בכלל ה-lint של הפרויקט). נבדק חי: גרירה סינתטית (dataTransfer) העבירה כרטיס מ"רוצה להזמין" ל"מתוכנן", המונים התעדכנו מיד, ואישרתי שהשינוי נשמר בפועל גם אחרי רענון מלא של הדף (לא רק תזוזה ויזואלית מקומית).
38. **חישוב מרחק/זמן נסיעה אוטומטי במסלול (OSRM):** שדות `RouteStop.distanceKm/travelTimeMinutes` היו קלט ידני בלבד. נוסף `RoutingProvider` (ממשק כמו `WeatherProvider`) עם מימוש `OsrmRoutingProvider` שקורא ל-`router.project-osrm.org` — שרת Demo ציבורי חינמי של OSRM, בלי מפתח API (3 בדיקות חדשות — סה"כ 103 בדיקות ב-data-layer). בטופס הוספת עצירה למסלול נוסף כפתור "📏 חשב מרחק וזמן אוטומטית (נהיגה)" שמופיע רק כשיש עצירה קודמת באותו יום עם קואורדינטות — קורא ל-Server Action, ממלא את שני השדות, והמשתמש עדיין יכול לערוך ידנית. כישלון/אין מסלול מציג הודעה כנה ("אפשר להזין ידנית"), לא ממציא ערך. נבדק חי: מלון בבנגקוק (13.7563,100.5018) → מסעדה (13.7367,100.5231) חושב אוטומטית ל-4.9 ק"מ / 9 דק' נהיגה (נתוני OSRM אמיתיים), נשמר בהצלחה ואושר ששרד רענון מלא של הדף.
39. **התראות דפדפן מקומיות (flight/taxi approaching):** `NotificationPreference` היה קיים בסכימת Prisma משלב 0 (10 סוגי אירוע ב-`NotificationEventType`) בלי מימוש. נוסף Repository מלא (Mock+Prisma+3 בדיקות) + מנוע תזכורות client-side טהור `dueReminders()` (4 בדיקות — סה"כ apps/web 40 בדיקות). **מכסה בפועל רק 2 מתוך 10 סוגי האירוע** — `flight_approaching`/`taxi_approaching`, היחידים עם timestamp מדויק בנתונים; שאר הסוגים (צ'ק-אאוט/ביטוח מסתיים/פיקדון/וכו') דורשים המצאת שעה שלא קיימת ולכן לא מומשו, ומתועדים ככה בכנות ב-UI. סעיף "התראות" חדש במסך הטיול (הפעלה+דקות-מראש לכל סוג נתמך). רכיב client במסך "עכשיו" מבקש הרשאת Notification API של הדפדפן ובודק כל 30 שניות מול נתוני הטיול הפעיל האמיתיים, עם דה-דופ ב-localStorage. **זו לא Web Push אמיתית** (לא מגיעה כשהטאב סגור) — דורש VAPID+Service Worker+שרת push, תשתית שלא מתאימה למבנה הנוכחי וגם נתקלת במגבלת רישום Service Worker הידועה בסביבת הבדיקה (ראה DECISIONS.md). נבדק חי מקצה לקצה: הרשאת "denied" האמיתית מוצגת בכנות; מסלול "granted" נבדק ב-monkey-patch (כמו ל-Geolocation בעבר) + טיסת בדיקה אמיתית שנוצרה דרך הטופס הרגיל עם departureAt 80 דקות מהרגע האמיתי — `Notification` נקראה בפועל עם הכותרת/תוכן הנכונים, ודה-דופ אומת שלא נורתה פעם שנייה בטעינה חוזרת. נתוני הבדיקה הוחזרו/נמחקו בסוף.
40. **דוח טיול — סינון עיר/מדינה + השוואה בין טיולים:** נוספו dropdown-ים לעיר/מדינה בדוח הטיול, עם אפשרויות שנגזרות מ-`Place.city`/`Place.country` של מקומות מקושרים לטיול בפועל (לא טקסט חופשי) — מסננים לפי `Expense.placeId` (שדה קיים, משמש כבר למסאז'ים/אוכל/קניות). הוצאה בלי מקום מקושר לא נכללת כשהסינון פעיל, עם הודעה מפורשת על כך. עמוד חדש `/trips/compare` משווה עלות כוללת/ממוצע ליום בין כל הטיולים הפעילים, מקובץ לפי מטבע בלי המרה מומצאת (אותו עיקרון כמו הדוח הבודד) — קישור "📊 השווה טיולים" מרשימת הטיולים. נבדק חי: קישרתי שני מקומות בערים שונות (בנגקוק/פאטאיה) והוצאה חדשה בפאטאיה — הסינון לפי "פאטאיה" צמצם נכון מ-2 הוצאות/550 THB ל-1 הוצאה/100 THB, בדיוק ההוצאה הנכונה; טבלת ההשוואה הציגה נכון שני טיולי דמו זה לצד זה עם מטבעות נפרדים. **זה הפריט הגדול-משמעותי האחרון בסקופ הפנימי (לא תלוי ב-Supabase/Storage).**
41. **תיקון גלישה אופקית במובייל (375 פיקסל):** לבקשת המשתמש ("המערכת תצטרך לעבוד גם במובייל וגם על מחשב") — בדיקה שיטתית חשפה שני מקורות גלישה אופקית אמיתית: (א) הניווט התחתון (7 קישורים) לא כיווץ נכון ברוחב צר — תוקן עם `flex:"1 1 0"`+`minWidth:0` לכל קישור וקיצור טקסט בשלוש נקודות כשאין מקום. (ב) בכל טופס עם שני שדות תאריך/שעה זה לצד זה (טיסה/מלון/ביטוח/השכרת רכב/תחבורה/מסלול/תכנון עתידי/יצירת-עריכת טיול — 9 טפסים) — שדות `date`/`datetime-local` לא מתכווצים מתחת לרוחב מינימלי של הדפדפן, ובזוג צמוד נדחסו מעבר לגבול המסך; תוקן עם `flexWrap:"wrap"`. ראה DECISIONS.md לפירוט המלא כולל הערה טכנית על מדדי JS לא-עקביים בכלי הבדיקה (הסתמכות סופית על צילומי מסך ויזואליים). נבדק חי במובייל (375px) ובדסקטופ (1280px) — אין עוד חיתוך/הצטופפות של שדות בשום מסך שנבדק.
42. **שער יציג חי (CurrencyRateProvider) + גילוי שסקשן "כספים" כבר היה מלא ותפקודי:** לבקשת המשתמש (יציאה לטיול עם כמה מטבעות בו-זמנית, לדעת בכל רגע כמה יש מכל מטבע מול שער חליפין אמיתי) — נוסף `CurrencyRateProvider` (כמו Weather/Routing: תמיד "אמיתי", לא תלוי ב-DATA_SOURCE): מקור ראשי בנק ישראל (`boi.org.il`, "שער יציג" רשמי, חינמי בלי מפתח, ~14 מטבעות), Fallback ל-Frankfurter (ECB, גם חינמי) למטבעות שבנק ישראל לא מפרסם (כמו THB) — מטבע שנכשל בשניהם פשוט לא מוצג, לעולם לא שער מומצא. **תוך כדי בדיקה התגלה שהתכנון המקורי (לבנות `finances/page.tsx` נפרד כי "המסך לא נגיש") היה שגוי** — סקשן "כספים" בעמוד הטיול (`trips/[tripId]/page.tsx`) כבר מרכיב הכל inline (בדיוק כמו bookings/documents) ותפקודי לגמרי; התוכנית תוקנה תוך כדי לשיפור המסך הקיים במקום route כפול. נוסף גם `listPaymentsByTrip` (מכסה רק תשלומים מקושרי-Expense) + כרטיס סיכום תשלומי אשראי לפי כרטיס+מטבע. **אגב כך תוקנו 5 קישורים מתים** ל-`/trips/[tripId]/finances` (route שמעולם לא היה קיים) בדשבורד וב-FAB — הוחלפו ל-`/trips/[tripId]#finances`. נבדק חי: ראה DECISIONS.md לפירוט המלא.
10. **אבטחה (קוד+עיצוב, לא הופעל live):** Publishable/Secret key separation, Admin client מוגן ב-`server-only`, RLS policies מלאות ל-38 טבלאות (`rls_policies.sql`), טריגר יצירת משתמש, תיקון פרצת userId חסר ב-Place/Contact/StatusHistory.
11. **Weather:** מחובר בפועל ל-Open-Meteo (חינמי, בלי API key — ראה DECISIONS.md). `OpenMeteoWeatherProvider` ממומש מלא (תנאים נוכחיים/תחזית שעתית/יומית), מוצג במסך "היום שלי" לפי מיקום המקום הראשון עם lat/lng שמקושר לטיול. המלצות לבוש/מטריה/אזהרת גשם לאופנוע/UV נגזרות מנתונים אמיתיים בלבד (`weather-advice.ts`). getAlerts() מחזיר [] בכנות (Open-Meteo חינמי לא חושף אזהרות גלובליות אמינות). נבדק חי: תנאים אמיתיים לבנגקוק (27.2°C, מרגיש כמו 31.7°, מעונן, לחות 79%) הופיעו נכון על מקום דמו עם קואורדינטות אמיתיות.

## מה עובד בפועל כרגע (אם תפתח http://localhost:3101)
כל 10 הסעיפים למעלה (1-9) עובדים end-to-end מול נתוני Demo. שרת הפיתוח **לא בהכרח רץ ברגע שאתה קורא את זה** — אם השרת לא רץ, יש להריץ מחדש (ראה "פעולה ראשונה" בסוף הקובץ).

## מה נבדק בפועל
- **בדיקות אוטומטיות:** 154/154 עוברות (`npm run test` מהשורש) — פירוט: apps/web 40 (logger 2, trip-days 5, gap-detection 12, weather-advice 7, opening-hours 5, haversine-distance 5, notification-reminders 4), packages/data-layer 106 (Trip 7, Place 5, Booking 15, Finance 23, TripPlace 3, Contact 4, PlannedActivity 10, Route 8, weather 8, Document 6, AuditLog 5, PaymentCard 3, StatusHistory 3, osrm-routing 3, notification-preference 3), packages/shared-types 8.
- **typecheck/build/lint:** ירוקים בכל commit (ראה "מצב Git" למטה).
- **בדיקה ידנית בדפדפן:** כל פיצ'ר שסומן "הושלם" למעלה נבדק ידנית בדפדפן האמיתי (לא רק בקוד) — כולל מסלולי הצלחה וכשל (validation errors).

## מה עדיין לא מחובר
- **Supabase חי** — URL/Publishable Key/Secret Key/DATABASE_URL עדיין לא סופקו. קבצי `.env.local`/`.env` נוצרו עם שמות המשתנים הנכונים אבל **ריקים**.
- **Migrations בפועל** — לא רץ אף `prisma migrate` מול DB אמיתי.
- **RLS בפועל** — כתוב ומוכן, לא הופעל.
- **Authentication אמיתי** — קוד מוכן, לא נבדק מול משתמש Supabase אמיתי.
- **Storage** — לא הוגדר בכלל (אין עדיין פיצ'ר שדורש אותו בפועל).

## מה נמצא ב-Demo בלבד
**כל הנתונים כרגע** — כולם ב-in-memory Mock repositories (`packages/data-layer/src/repositories/*.mock.ts`). מתאפסים בכל הפעלה מחדש של שרת הפיתוח (חוזרים לנתוני Seed: 2 טיולי דמו, 3 מקומות דמו, מלון+טיסה+הוצאה+ארנק דמו). זה תקין ומכוון — לא באג.

## מה עדיין חסר (רשימה מלאה: PROJECT_REQUIREMENTS.md)
עיקרי: התראות Web Push (Not Started, צפוי להיתקל במגבלת רישום Service Worker בסביבת הבדיקה — ראה סעיף "בעיות שהתגלו"), דוחות — סינון לפי עיר/מדינה + השוואה בין טיולים (אופציונלי, קטן). מעבר לזה, כל השאר תלוי בחיבור Supabase חי (RLS/Auth/DB אמיתי) ו-Storage+Claude API key (OCR) — שני חסמים חיצוניים בלבד, שמורים לדיון עם המשתמש בסוף.

---

## דרישות חשובות של המשתמש (תמצית — הרחבה מלאה ב-PROJECT_REQUIREMENTS.md)
- לא לבנות הכל בבת אחת; שלבים מסודרים עם בדיקות בסוף כל שלב.
- אין קוד/UI שרק "נראה" עובד; Demo Data מסומן בבירור, לא בפרודקשן.
- Soft Delete בלבד, לא מחיקה בלתי הפיכה.
- RLS אמיתי לפני שנחשיב CRUD "מוכן" — כולל בדיקה מול שני משתמשי Test אמיתיים (עדיין לא בוצע, תלוי Supabase).
- Git מקומי בלבד, לעולם לא Remote/Push.
- Secrets לעולם לא בצ'אט/קוד/Git/Logs.
- כל עבודה רק בתוך `C:\Users\ארנון\OneDrive\Desktop\קלוד קוד\טיולים`.
- **(חדש, 2026-08-15) מנגנון קבצי המשכיות זה עצמו** — לעדכן את שלושת הקבצים האלה אחרי כל שלב משמעותי, לפני Compact, ובתחילת כל Session.

## בעיות שהתגלו ופתרונות שכבר נוסו
1. **Prisma Client לא ניתן לייבוא** — generator לא הגדיר `output` מפורש → `require('@travel-app/db')` נכשל. **תוקן:** נוסף `output = "../generated/client"`.
2. **CSS רספונסיבי לא עבד** — `style` inline על ה-nav התחתון דרס את ה-media query. **תוקן:** הוסר ה-`display` מה-inline style, נשאר רק ב-CSS class.
3. **ESLint קרס עם "Converting circular structure to JSON"** — ניסיון לעטוף flat config מוכן (`eslint-config-next` v16) עם `FlatCompat` (מיועד לפורמט ישן). **תוקן:** ייבוא ישיר של ה-flat config.
4. **Build נכשל לסירוגין עם `EPERM: operation not permitted, unlink`** על `.next\static\...` — OneDrive מסנכרן/נועל קבצים תוך כדי ה-build. **פתרון זמני:** מחיקת `.next` והרצה חוזרת. **פתרון מומלץ למשתמש (לא בוצע):** להחריג `apps\web\.next` מסנכרון OneDrive.
5. **Windows Explorer לא יוצר קובץ שמתחיל בנקודה** (`.env.local`) בקלות. **פתרון:** Claude יצר את הקבצים מראש עם שמות המשתנים וערכים ריקים; המשתמש רק ממלא ב-Notepad.
6. **בדיקת RLS על טבלאות ללא userId** — Place/Contact/StatusHistory היו חסרות `userId`, פרצת בידוד פוטנציאלית. **תוקן** בסכימה לפני שהיו נתונים אמיתיים.
8. **קישורים מתים ל-`/trips/[tripId]/bookings` ו-`/trips/[tripId]/documents`** — אותה מחלקת באג שתוקנה עבור `/finances` (ראה סעיף 42 למעלה, DECISIONS.md 2026-08-16): הן מורכבות inline בעמוד הטיול הראשי, לא routes עצמאיים, אז קישורים אליהן (למשל ב-`quick-add-fab.tsx`/`quick-actions-row.tsx`) מובילים ל-404. **לא תוקן עדיין** — מחוץ להיקף הסבב הזה, מתועד כפריט פתוח.
7. **רישום Service Worker נכשל בכלי הדפדפן האוטומטי** — `navigator.serviceWorker.register('/sw.js')` נכשל עם "An unknown error occurred when fetching the script", למרות ש-`curl` מאשר שהקובץ מוגש נכון (200, `Content-Type: application/javascript`, secure context localhost). **לא תוקן — כנראה מגבלת סביבת הבדיקה (headless), לא באג בקוד.** באנר "לא מקוון" עצמו כן אומת עובד (toggled navigator.onLine + dispatched events בדפדפן). מומלץ לבדוק רישום SW בדפדפן אמיתי.

## דברים שאסור לעשות (תזכורת)
- לגעת בתיקיית "אפליקציית ניהול מוניות" או כל תיקייה מחוץ ל-`טיולים`.
- git remote / git push.
- להציג/לרשום ערכי Secret (מפתחות, סיסמאות, Connection String מלא) בצ'אט, בקוד, או בקבצי ההמשכיות האלה.
- לטעון שנבדק RLS/Auth/Persistence/Migration אמיתי מול Supabase — הם עדיין לא מחוברים.
- להשתמש ב-`prisma db push` כתחליף להיסטוריית migrations (רק `migrate dev`/`migrate deploy`, ורק אחרי אישור שזו סביבת Development).
- להציג נתון מזג אוויר מומצא/דמה כאילו הוא Forecast אמיתי.

## תלויות חיצוניות שחסרות
- **Supabase**: Project URL, Publishable Key, Secret Key, Database Connection String — טרם סופקו ע"י המשתמש.
- **Claude API key** (ל-OCR מסמכים) — יידרש בשלב 4, טרם התבקש.
- **Mapbox Public Token** (`NEXT_PUBLIC_MAPBOX_TOKEN`, 2026-08-16) — חינמי עד 50K טעינות/חודש, נדרש כדי לראות מפה תלת-ממדית חיה במסך `/map-demo`. המשתמש הצהיר שיפתח חשבון ויספק כשנגיע לשלב הבדיקה. עד אז `/map-demo` מציג מצב "לא מחובר" כן.

(מפה ומזג אוויר **לא** ברשימה הזו יותר — שניהם נפתרו עם ספקים חינמיים ללא API key: Leaflet+OpenStreetMap ו-Open-Meteo, בהתאמה. ראה DECISIONS.md.)

---

## מצב Supabase
**מחובר בפועל ומאומת (2026-08-17).** Project URL + Publishable Key + Secret Key + סיסמת DB — כולם אומתו מול API חי. `apps/web/.env.local` ו-`packages/db/.env` מלאים בערכים האמיתיים — שני הקבצים מוחרגים מ-Git. `NEXT_PUBLIC_MAPBOX_TOKEN` עדיין ריק (לא סופק).
**⚠️ הערת אבטחה:** ה-Secret Key והסיסמה הודבקו בצ'אט ע"י המשתמש. אפשר לסובב (Rotate) בדשבורד של Supabase בכל שלב אם רוצים; עדכן את שני קבצי ה-.env בהתאם.
**RLS פעיל בפועל ומאומת (2026-08-17).** ר' "מצב Database" למטה לפרטי הבאג שנמצא ותוקן בדרך, ו-DECISIONS.md לתיעוד המלא של תהליך האימות.
**מה עוד נדרש להשלמת חיבור מלא:** להעביר `DATA_SOURCE` מ-`mock` ל-`prisma` (**עדיין לא בוצע — ממתין לאישור המשתמש**, כי זו נקודה שדורשת ממנו להירשם/להתחבר עם חשבון אמיתי כדי להמשיך להשתמש באפליקציה בכלל — אין יותר נתוני [דמו] מיידיים אחרי המעבר).

## מצב Database
Schema מלא, `prisma generate` עובר נקי. **DB חי ב-Supabase, מיגרציות עד ותוך כדי שלב A/E הורצו בהצלחה בפועל** (`prisma migrate status` אישר בעבר "Database schema is up to date") — כולל, בין השאר: `20260817101712_init` (38 טבלאות), `20260817102045_enable_rls` (RLS+טריגר), `20260817110000_trip_medical_notes_and_passport_document_type`, ומיגרציות נוספות שנוספו לאורך שלב A (רשימה מלאה: `ls packages/db/prisma/migrations/` — אל תסמוך על רשימה ידנית כאן, היא נוטה להתיישן).
**⚠️ 10 migrations חדשים טרם הוחלו על ה-DB החי** (נוספו בסבבים 10-13, `prisma migrate deploy` נכשל עם P1001 — Supabase לא הגיב, כנראה נכנס למצב שינה בטייר החינמי אחרי חוסר-פעילות):
- `20260821140000_document_trip_id_nullable` ו-`20260821140100_document_place_photos_rls` (סבב 10 — Document.tripId nullable + תיקון RLS למקומות)
- `20260821150000_refund_is_received` (סבב 11 — Refund-Pending)
- `20260821160000_place_category_river_entertainment` (סבב 12 — קטגוריות "נחל"/"בילוי")
- `20260822090000_expense_participants` (סבב 13, חלק G — טבלת ExpenseParticipant + RLS לחלוקת-הוצאות)
- `20260822100000_trip_share_links` (סבב 13, חלק H — טבלת TripShareLink + RLS לשיתוף-מסלול ציבורי)
- `20260822110000_document_entity_type_trip_day` (סבב-2 חלק A — ערך enum חדש ל-DocumentEntityType)
- `20260822120000_trip_type` (סבב-2 חלק D — Trip.tripType nullable + enum TripType)
- `20260822130000_loyalty_programs` (סבב-2 חלק E — טבלת LoyaltyProgram + RLS)
- `20260822140000_companion_polls` (סבב-2 חלק G — טבלאות CompanionPoll/Option/Vote + RLS)

**לפני מעבר ל-DATA_SOURCE=prisma חובה להריץ `prisma migrate deploy` ולוודא שכל עשרתם הצליחו** — אחרת ה-Prisma Client (שכבר עודכן ל-schema החדש) יצפה למבנה/ערכים שעדיין לא קיימים בפועל ב-DB, וזה ייכשל.
**אימות RLS אמיתי (לא רק "רץ בלי שגיאה"), 2026-08-17:** ניסיון INSERT אנונימי ל-3 טבלאות בדפוסי-policy שונים (trips/hotel_stays/places) — כולם נדחו נכון עם `42501`. הרשמה אמיתית דרך אותו endpoint ש-`/register` קורא לו — הטריגר יצר שורת `public.users` נכונה. משתמש הבדיקה נמחק בסוף.
**באג נוסף שהתגלה, לא תוקן (לא דחוף):** מחיקת `auth.users` לא מוחקת cascade את `public.users` המתאים — יש רק Trigger ל-INSERT, לא ל-DELETE. רלוונטי רק כשתיבנה "מחק את החשבון שלי".
**חשוב לכל migration עתידי נגד ה-DB הזה:** להשתמש רק ב-`prisma migrate deploy`, **לא** `migrate dev` — `migrate dev` תמיד מריץ Shadow Database לאימות, ומיגרציית ה-RLS מפנה ל-`auth.users` שלא קיים שם → נכשל תמיד.

## מצב Authentication
קוד מוכן: `createSupabaseBrowserClient`/`createSupabaseServerClient`/`createSupabaseAdminClient`, מפרידים Publishable/Secret כראוי. `getCurrentUser()` עוקף לחלוטין למשתמש דמו קבוע כש-`DATA_SOURCE=mock` (עדיין המצב הנוכחי).
**נבדק חי בפועל (2026-08-17):** הרשמה אמיתית (`/auth/v1/signup`, אותו endpoint ש-`/register` קורא לו) + הטריגר שיוצר `public.users` — עובד נכון מקצה לקצה (id/email/locale/legalConsentAcceptedAt כולם נכונים). ניסיון Login לפני אישור אימייל נדחה כראוי (`email_not_confirmed`) — מאשר שהגדרת אישור-אימייל-חובה של הפרויקט פעילה. **לא נבדק עדיין:** Login אחרי אישור אימייל בפועל (המשתמש יצטרך לעבור את זה בעצמו כשירשם עם חשבון אמיתי משלו — לא נבדק עם חשבון בדיקה כי זה דורש לפתוח אימייל ולאשר).
**נוסף (2026-08-16):** מסך `/register` (הרשמה, `signUp`) לצד `/login` הקיים (`signInWithPassword`) — אימייל=שם משתמש, checkbox חובה לאישור תקנון/הסכם/מדיניות פרטיות (3 מסמכים תחת `/legal/*`, טיוטה שלא נבדקה משפטית).

## מצב Weather
מחובר בפועל ל-Open-Meteo (חינמי, בלי API key). UI במסך "היום שלי" — תנאים נוכחיים + המלצות לבוש/גשם/UV + **תחזית שעתית (8 שעות) ויומית (5 ימים)** — הושלם. נבדק חי עם נתונים אמיתיים לבנגקוק. getAlerts() תמיד [] (מגבלת הספק החינמי — ראה DECISIONS.md).

## מצב Maps
מחובר בפועל — מסך `/map` (Leaflet+OpenStreetMap, חינמי) + כפתורי ניווט Google Maps/Waze בכל מקום שרלוונטי (Places, TripPlace, Route stops). "מה קרוב אליי" (דורש הרשאת מיקום) עדיין לא ממומש. `/map` הקיים **לא נגע** בעבודת העיצוב המחודש (2026-08-16) ונשאר כמו שהוא.
**עיצוב Premium (2026-08-16, In Progress):** הפשטת `MapProvider` חדשה ב-`apps/web/lib/map/` + מסך `/map-demo` שמוכיח מפה תלת-ממדית עם Mapbox GL JS (Standard style) — ראה DECISIONS.md לבחירת הספק על פני Google Photorealistic 3D Tiles ו-PROJECT_REQUIREMENTS.md סעיף 38ה-38ו לפירוט. `/map-demo` לא בניווט עדיין, לא מחליף את `/map`. דורש `NEXT_PUBLIC_MAPBOX_TOKEN` שטרם סופק — עד אז נבדק רק במצב "לא מחובר".

## מצב Theme (חדש, 2026-08-16, In Progress)
עד עכשיו: פלטה כהה אחת קבועה (`apps/web/app/globals.css`), בלי מנגנון Light/Dark/Auto. עיצוב Premium מוסיף מערכת Theme מלאה (CSS custom properties + `ThemeProvider` + מסך `/settings`) — ראה PROJECT_REQUIREMENTS.md סעיף 38א ו-DECISIONS.md.

## מצב Documents
התחיל (2026-08-15) — קבלות על הוצאות עובד חי (Repository מלא + UI). קובץ שמור כ-`data:` URI אמיתי בזיכרון ה-Mock (לא Storage אמיתי — ראה DECISIONS.md). OCR לא רץ בפועל. שאר סוגי הישות (Booking/Insurance/Place/וכו') נתמכים במודל אבל אין UI עדיין. תלוי ב-Supabase Storage אמיתי כדי לעבור לפרודקשן.

## מצב Offline
התחיל (2026-08-15), בהיקף מוגבל בכוונה — PWA shell caching (Service Worker + manifest) + באנר "לא מקוון", לא סנכרון דו-כיווני אמיתי (ראה DECISIONS.md — דורש שינוי ארכיטקטוני, לא בוצע בלי אישור). רישום ה-Service Worker בפועל לא אומת בכלי הבדיקה האוטומטי (ראה "בעיות שהתגלו" למטה) — כדאי בדיקה בדפדפן אמיתי כשנוח.

## מצב Wallet
Mock מלא + UI. תשלום מזומן מפחית אוטומטית — נבדק חי. **Currency Exchange** מעדכן שני ארנקים אוטומטית (יוצר ארנק חדש אם צריך) — נבדק חי. Prisma implementation כתוב (עם `$transaction`) לשניהם, לא נבדק live. **היסטוריית תנועות ארנק (WalletTransaction)** — הושלם, כל פעולה על הארנק (טעינה/מזומן/המרה/החזר/פיקדון) נרשמת ומוצגת ממוינת מהחדש לישן. **Deposit (פיקדון)** — הושלם, כולל "סמן כהוחזר" — נבדק חי.

## מצב Trips
Mock מלא + UI מלא (CRUD, תאריכים, Soft Delete/שחזור). Prisma implementation כתוב, לא נבדק live.

## מצב Places
Mock מלא + UI (רשימה/סינון/יצירה/מחיקה). Prisma implementation כתוב, לא נבדק live. **שעות פתיחה + "פתוח עכשיו"** — הושלם: עורך שעות בטופס, תג פתוח/סגור בעמוד המקומות ובמקומות המקושרים לטיול — נבדק חי.

## מצב Bookings (מלונות/טיסות/תחבורה/ביטוח/השכרת רכב)
Mock מלא + UI (רשימה+יצירה לכל סוג, כולל Insurance ו-CarRental — 5 סוגים). Prisma implementation כתוב, לא נבדק live. אזורי זמן מטופלים כפשטנות (UTC ישיר) — ראה DECISIONS.md.

## מצב Expenses
Mock מלא + UI בסיסי (קטגוריה/סכום/מטבע/תיאור). Prisma implementation כתוב.

## מצב Payments
Mock מלא + UI. אפקט אוטומטי על Wallet לתשלום מזומן — נבדק. Prisma implementation כתוב עם transaction. **קישור לכרטיס אשראי (PaymentCard)** — הושלם: טופס התשלום מציג בחירת כרטיס כש-paymentMethod=credit_card — נבדק חי.

## מצב PaymentCard
Mock מלא + UI (רשימה+יצירה, מרכז גלובלי לא לפי טיול, כמו Contacts). אין Soft Delete (אין deletedAt בסכימה). Prisma implementation כתוב, לא נבדק live. מקושר בפועל ל-Payment.cardId.

## מצב Hotels / Flights / Transport
ראה "מצב Bookings" למעלה — כולם תחת אותו Repository/UI.

## מצב Contacts
Mock מלא + UI (רשימה/יצירה/מחיקה, מרכז גלובלי לא לפי טיול). Prisma implementation כתוב, לא נבדק live.

## מצב Reports
דוח טיול בסיסי (`/trips/[tripId]/report`) — הושלם, כולל כרטיס "תכנון מול ביצוע" חדש (StatusHistory). סינון לפי טווח/עיר/מדינה, גרפים ויזואליים, השוואה בין טיולים — Not Started.

## מצב StatusHistory
Mock מלא + Prisma כתוב (לא נבדק live), מחובר בפועל רק לשינוי סטטוס PlannedActivity (כמו AuditLog — Coverage חלקי בכוונה). דוח "תכנון מול ביצוע" בעמוד דוח הטיול — נבדק חי.

---

## מצב Git
ריפו מקומי בלבד (`git init` ב-`C:\Users\ארנון\OneDrive\Desktop\קלוד קוד\טיולים`), **אין Remote**, לא בוצע Push מעולם.

### Commit אחרון
`e74c0bf` — "שכפול טיול (כולל מסלול יומי מלא) + הצבעות בין המלווים (בקשת משתמש)" — חלקים F-G, **האחרונים בסבב-2** של חבילת-הפיצ'רים המורחבת (ר' `C:\Users\ארנון\.claude\plans\groovy-wibbling-perlis.md`, **כל 7 החלקים A-G הושלמו**): שכפול טיול כולל מסלול יומי מלא (RouteStop-ים בהזזת-תאריכים 1:1), והצבעות בין המלווים (CompanionPoll, proxy voting — בעל החשבון מזין בשם המלווים). שני החלקים אומתו חי מול שרת-הפיתוח הרץ, לא רק unit tests. חלקים A-E (תמונות-יום/`/stats`/אזהרת-תקציב/`Trip.tripType`/`/loyalty-programs`) הושלמו קודם ב-`b4a1bc9`. **סבב-1 (חלקים A-H, כל 8 הושלמו) הסתיים לפני כן ב-`4e0ab47`** — ר' commit ההוא לפרטי #88 Share Security/מלווים/חלוקת-הוצאות/ממיר-מטבע וכו'.

**היסטוריית commits מלאה:** `git log --oneline` — לא מתועדת כאן שורה-שורה יותר (הרשימה הפכה ארוכה מדי לתחזוקה). סבבים עיקריים מתועדים ב-FEATURE_AUDIT.md ("עדכון YYYY-MM-DD (סבב N)") ו-DECISIONS.md, כל אחד עם רשימת "משפיע על" מלאה.
**⚠️ 10 migrations לא-מוחלים על ה-DB החי** (ר' "מצב Database" למטה) — עדיין רלוונטי, חובה `prisma migrate deploy` לפני DATA_SOURCE=prisma.

### 96 ה-commits עד כה (מהישן לחדש)
1. `1fb0e64` — Initial commit: Phase 0 infra + shared-types + Supabase security prep
2. `9060904` — Data Layer abstraction + תיקון Prisma output path
3. `41c37c0` — App Shell
4. `42304af` — Trip CRUD מלא
5. `0b43951` — Places
6. `3c99eff` — Bookings
7. `b047405` — Finances
8. `5189229` — TripPlace + "היום שלי"
9. `740bc3d` — קבצי המשכיות (PROJECT_STATE/REQUIREMENTS/DECISIONS) נוצרו
10. `a1a2421` — Currency Exchange + עדכון קבצי המשכיות
11. `c0fdd6b` — תיקון הפניית hash
12. `347e87a` — Tips (מקבל+קטגוריה) + דוח טיפים
13. `ca595da` — Contacts (מרכז אנשי קשר גלובלי)
14. `04f022c` — Insurance (סוג Booking רביעי)
15. `c5d9f07` — Refund (מזכה ארנק אוטומטית)
16. `8426e44` — תיקון הפניית hash
17. `a454733` — תיקון שורות כפולות ב-PROJECT_STATE.md
18. `4d07f61` — קטגוריות הוצאה דינמיות (String חופשי + datalist, לא עוד enum)
19. `f879575` — תיקון הפניית hash
20. `80dff9f` — Place: כתובת/lat/lng + כפתורי ניווט Google Maps/Waze
21. `d92e8ee` — עדכון קבצי המשכיות (Place lat/lng + ניווט)
22. `6f8e5c4` — תיקון הפניית hash
23. `ec7e00a` — מסך מפה בסיסי (Leaflet + OpenStreetMap)
24. `85d74f7` — עדכון קבצי המשכיות (מסך מפה)
25. `ba4706a` — תיקון הפניית hash
26. `563fa92` — שדות ייעודיים למסאז'ים/אוכל/פירות/קניות/אטרקציות
27. `9e28f2e` — עדכון קבצי המשכיות (שדות ייעודיים)
28. `c34bb75` — תיקון הפניית hash
29. `1d014a0` — מסך "עכשיו"
30. `31a4e55` — עדכון קבצי המשכיות (מסך עכשיו)
31. `8a4932b` — תיקון הפניית hash
32. `a39c86e` — דוח טיול בסיסי
33. `decce8b` — עדכון קבצי המשכיות (דוח טיול)
34. `2ea97a0` — תיקון הפניית hash
35. `81511d0` — Planned Activity (תכנון עתידי)
36. `7270622` — עדכון קבצי המשכיות (Planned Activity)
37. `0fb4d44` — תיקון הפניית hash
38. `36c0bcd` — מסלול יומי (Route)
39. `e00bf7f` — עדכון קבצי המשכיות (Route)
40. `fbe8b6c` — תיקון הפניית hash
41. `6297058` — מנוע בדיקת חוסרים (Gap Detection) — משלים את שלב 2
42. `bc3082b` — עדכון קבצי המשכיות (סיום שלב 2)
43. `6572c2c` — Weather — חיבור אמיתי ל-Open-Meteo
44. `e02b3ac` — עדכון קבצי המשכיות (Weather) + תיקון סעיף TODO מיושן
45. `4ac152f` — תיקון הפניית hash
46. `08e0abe` — Documents (קבלות על הוצאות)
47. `43def2b` — עדכון קבצי המשכיות (Documents)
48. `264d520` — תיקון הפניית hash
49. `4440cc0` — Offline בסיסי (PWA shell + באנר לא-מקוון)
50. `13e217d` — עדכון קבצי המשכיות (Offline, כל 5 השלבים נגעו)
51. `7337b0e` — Audit Log (יומן שינויים) — Trip + PlannedActivity
52. `3674af9` — עדכון קבצי המשכיות (Audit Log)
53. `a42af6c` — תיקון הפניית hash
54. `30a1098` — Soft Delete ל-Expense + 4 סוגי Booking
55. `238b96f` — עדכון קבצי המשכיות (Soft Delete)
56. `3328f31` — היסטוריית תנועות ארנק + Deposit (פיקדון)
57. `4e2580e` — עדכון קבצי המשכיות (היסטוריית תנועות ארנק + Deposit)
58. `8d1c65e` — תיקון הפניית hash
59. `f2df5ea` — PaymentCard (אמצעי תשלום/כרטיסי אשראי) + קישור ל-Payment
60. `b5cce64` — עדכון קבצי המשכיות (PaymentCard)
61. `3397348` — תיקון הפניית hash
62. `84c3bfa` — StatusHistory + דוח "תכנון מול ביצוע"
63. `e88b77f` — עדכון קבצי המשכיות (StatusHistory)
64. `92b9cfe` — תיקון הפניית hash
65. `b7c2a2e` — שעות פתיחה + "פתוח עכשיו" ל-Place
66. `60dda5f` — עדכון קבצי המשכיות (שעות פתיחה)
67. `9119a34` — תיקון הפניית hash
68. `a51e865` — סינון טיולים לפי טווח תאריכים
69. `eee531a` — עדכון קבצי המשכיות (סינון טווח תאריכים)
70. `1875244` — תיקון הפניית hash
71. `b024b5a` — תחזית שעתית/יומית במסך "היום שלי"
72. `80a0b38` — עדכון קבצי המשכיות (תחזית שעתית/יומית)
73. `0e61935` — תיקון הפניית hash
74. `ae0a764` — השכרת רכב/אופנוע (CarRental) — סוג Booking חמישי
75. `b1516cf` — עדכון קבצי המשכיות (CarRental)
76. `68ce6c7` — תיקון הפניית hash
77. `ef1e154` — המרת Planned Activity ל-Booking אמיתי
78. `e6825c4` — עדכון קבצי המשכיות (המרת Planned Activity)
79. `481ebd3` — תיקון הפניית hash
80. `f50f05a` — "מקומות קרובים אליי" במסך עכשיו (Geolocation)
81. `90863ee` — עדכון קבצי המשכיות (מקומות קרובים)
82. `8ec5fdc` — תיקון הפניית hash
83. `3f3da28` — דוח טיול — סינון תאריכים + גרפים
84. `0d47243` — עדכון קבצי המשכיות (דוח טיול)
85. `2b9b432` — תיקון הפניית hash
86. `59a6877` — קנבן גרירה ל-Planned Activity
87. `d983b7e` — עדכון קבצי המשכיות (קנבן)
88. `222ca11` — חישוב מרחק/זמן נסיעה אוטומטי במסלול (OSRM)
89. `e175363` — עדכון קבצי המשכיות (OSRM)
90. `40228e5` — התראות דפדפן מקומיות (flight/taxi approaching)
91. `6577d8b` — עדכון קבצי המשכיות (התראות)
92. `7c7ffb7` — דוח טיול: סינון עיר/מדינה + השוואה בין טיולים
93. `d008d34` — עדכון קבצי המשכיות (דוח טיול: עיר/מדינה + השוואה)
94. `4477b27` — תיקון גלישה אופקית במובייל (ניווט תחתון + זוגות שדות תאריך)
95. `aee0c5c` — עדכון קבצי המשכיות (תיקון גלישה אופקית במובייל)
96. `966f094` — תיקון הפניית hash
97. `0ee5b50` — עיצוב Premium: Theme + Nav + Dashboard + MapProvider + Demo Map

### מצב עבודה נוכחי
**2026-08-16 (עדכון אחרון) — סבב רביעי: פרטי-עיצוב לפי גרסה שנייה של תמונת הייחוס + שינוי שיטת עבודה.** המשתמש דרש Screenshot-based verification שאין לי יכולת לספק (אין כלי דפדפן בסביבה) — **הוסכם מפורשות שמעכשיו המשתמש ישלח צילומי מסך של `localhost:3101/dashboard` בעצמו**, כדי לאפשר השוואה אמיתית במקום ניחוש עיוור. נוספו: Progress Ring אמיתי (הוצאות/יתרת ארנק) בכרטיס הוצאות, Demo Markers דקורטיביים על תמונת ה-Demo של המפה, בלוק Profile+מתג Dark Mode אמיתי ב-Sidebar, Quick Actions 5 פריטים (כולל "נווט" עם קישור Google Maps אמיתי), קיצור מקלדת ⌘K אמיתי בחיפוש. **תוקן באג build אמיתי** (ייבוא לא-תקין של `react-dom/server` דרך `marker-style.ts` לתוך Server Component) — נתפס רק ב-`npm run build`, לא ב-typecheck. ראה DECISIONS.md לפירוט. typecheck/lint/build/163 בדיקות ירוקים, נבדק ידנית.

**היסטוריה (סבב שלישי, לפני עדכון זה): אחרי שני סבבי כיוונון Tokens שעדיין "הרגישו כמו Admin Panel", אובחן שהבעיה האמיתית הייתה **תוכן ריק** — ה-Hero והמפה, שני האלמנטים הכי דומיננטיים בתמונת הייחוס, היו גרדיאנט/Placeholder בלבד אצלנו, ול-Glass Effect לא היה רקע צבעוני לטשטש. נוספו תמונות נוף אמיתיות מ-Wikimedia Commons (נבחרות לפי יעד הטיול בפועל, מאומתות HTTP 200, קרדיט מוצג) ל-3 מקומות (Hero/Map Demo/Tip thumbnail) + שכבת Ambient Glow ברקע כל האפליקציה. ראה DECISIONS.md ("עדיין נראה כמו Admin Panel") לפירוט. typecheck/lint/build/163 בדיקות ירוקים, נבדק ידנית כולל אימות ישיר שכתובות התמונות עונות 200.

**היסטוריה (סבב שני, לפני עדכון זה): המשתמש קבע שה-POC הקודם (`ea29cff`) לא היה בכיוון הנכון. בוצע ניתוח שיטתי של תמונת הייחוס (מתועד למשתמש לפני קוד) והושוו/תוקנו: יחס Hero:Map (1:2.35), סדר תוכן בכרטיס הטיול + שורת מזג אוויר, Placeholder מפה גדול יותר עם תווית "Demo" מפורשת, טוקנים כהים/סגולים-דומיננטיים יותר עם glass עדין יותר, טיפוגרפיית מספרים גדולה יותר, כרטיסי סטטיסטיקה מגוונים, ומבנה שורות 3-4 תואם למוקאפ (5 כרטיסים / 3 כרטיסים). typecheck/lint/build/160 בדיקות ירוקים, נבדק ידנית. **פערים כנים שנותרו**: רשימת "My Wallet" נשארה בתוכן הראשי (לא הועברה ל-Sidebar כמו במוקאפ — שינוי גדול יותר, ממתין להנחיה), והמפה עצמה עדיין Placeholder (תלוי ב-Mapbox token). ראה DECISIONS.md לפירוט המלא.

**היסטוריה (לפני עדכון זה, מהחדש לישן):**
- Design System v2 מלא (Tokens/Rubik/ספריית `components/ui/`) + POC דשבורד ראשון (`ea29cff`) — הוחלף/שודרג על ידי הסבב השני שתועד למעלה, לא נזרק.
- לפני זה: שלבים a-f של עיצוב Premium (Theme/Nav/MapProvider/Demo Map) + שני סבבי סגירת פערים (כותרת/חיפוש/Donut/תובנות/Widget מפה/טיפ יומי) + מסכי הרשמה/משפטי (`/register`, `/legal/*`). תוכנית מקורית: `C:\Users\ארנון\.claude\plans\glistening-knitting-scott.md`.
- **קבוע לאורך כל הסבבים:** בדיקה חזותית אמיתית בדפדפן לא בוצעה (אין כלי אוטומציה זמין), בדיקת המפה עם Mapbox token אמיתי טרם בוצעה (טרם סופק), הרשמה/התחברות מול Supabase live טרם נבדקה (טרם מחובר). לא הורחב מעבר לדשבורד+ניווט+theme+map infra — לא נגעו מסכי טיולים/מקומות/הזמנות/אנשי קשר, לא נוסף `/map-demo` לניווט, לא הוחלף `/map` הקיים.

## תוצאות הבדיקות האחרונות
`npm run test` מהשורש: **170/170 עוברות** (כולל 5 בדיקות חדשות ל-`CurrencyRateProvider`). `npm run typecheck`: נקי. `npm run lint`: נקי (0 שגיאות/אזהרות). `npm run build`: עובר (exit 0), עם אזהרת bundling לא-חוסמת (ראה "שגיאות פתוחות" למטה). 13 routes מרכזיים נבדקו חי (curl, כולם 200).

## שגיאות פתוחות (לא חוסמות)
1. Build warning: "Dynamic filesystem access causes tracing of the whole project" — Turbopack עוקב אחרי `@travel-app/db` (Prisma) גם ב-mock mode כי ה-`import()` הדינמי עדיין מזוהה סטטית. לא משפיע על נכונות, רק על גודל bundle עתידי ב-deploy. לטפל כשמחברים Prisma אמיתי.
2. OneDrive EPERM על `.next` — ראה "בעיות שהתגלו" סעיף 4.
3. Timezone handling ב-Bookings פשטני (UTC ישיר) — ראה DECISIONS.md.

---

## TODO מסודר לפי עדיפות
0. **[בעבודה כרגע, 2026-08-16]** עיצוב Premium — Theme/Nav/Dashboard/MapProvider/Demo Map, לפי התוכנית שאושרה (`C:\Users\ארנון\.claude\plans\glistening-knitting-scott.md`, PROJECT_REQUIREMENTS.md סעיף 38). בסיום: typecheck/lint/test/build + בדיקה ידנית, דיווח מרוכז למשתמש, ואז המתנה לאישור לפני commit או שלב נוסף (לא ממשיכים לשכפל את העיצוב לשאר המסכים בלי אישור).
1. **[עוצר את המשתמש]** לקבל פרטי Supabase (URL/Publishable Key בצ'אט; Secret/DATABASE_URL ישירות בקבצים) → לבדוק טעינת env בפועל → `prisma migrate dev` → הפעלת RLS → בדיקת שני משתמשי Test → Trip CRUD אמיתי מול DB.
2. Storage אמיתי + Claude API key ל-OCR — חסום, ברשימת השאלות לסוף.
3. **כל הפריטים הגדולים-משמעותיים שלא תלויים בשני החסמים האלה הושלמו.** מה שנשאר (8 מתוך 10 סוגי התראה, Web Push אמיתי) הוגדר ותועד בכוונה כ-Not Started — לא פער שנשכח, אלא תלוי בתשתית שלא מתאימה למבנה הנוכחי (ראה DECISIONS.md "מצב Notifications").

## דרישות שלי שעדיין לא מומשו
ראה `PROJECT_REQUIREMENTS.md` המלא לרשימה עם סטטוס לכל פריט. עיקריים שעדיין **Not Started** לגמרי: Documents/OCR (חסום ע"י Storage), 8 מתוך 10 סוגי ההתראה (דורשים timestamp שלא קיים בנתונים), Web Push אמיתי (VAPID+Service Worker+שרת — לא רק Notification API מקומי).

## עדכון 2026-08-17 (סבב חדש) — שלב A מלא, לפי קובץ אפיון.docx + חיבור Supabase חלקי

המשתמש סיפק "איפיון אפליקציה.docx" (שמור ב-Git) — מסמך אפיון מאוחד שמגדיר תהליך עבודה מחייב וסדר שלבים A–F. ביקש שאמשיך את שלב A (השלמת כל 48 ה-PARTIAL וכל 8 ה-NOT IMPLEMENTED שלא תלויים בשירות חיצוני) עצמאית, לפני שלב עיצוב שיגיע בהמשך (C/D) וחיבור שירותים חיצוניים (E).

**בנוסף, לפני שלב A, המשתמש ביקש להתחיל לחבר Supabase** (חריגה מסודר השלבים, לבקשתו המפורשת). סופקו Project URL + Publishable Key + Secret Key + סיסמת DB (כולם בצ'אט — לא נמנע, מתועד). אומתו מול API חי (Auth health 200, PostgREST 200). `npx prisma migrate dev --name init` רץ בהצלחה — **DB אמיתי חי עם 38 טבלאות**, אומת בנוסף בשאילתת REST ישירה מול `trips` (200, `[]`). המשתמש ביקש לעצור את עבודת החיבורים בשלב הזה (RLS עדיין לא הופעל, DATA_SOURCE נשאר mock) כדי להתמקד בקובץ האפיון. ר' "מצב Supabase"/"מצב Database" למעלה לפרטים המלאים כולל איך להמשיך.

**שלב A בוצע ב-10 סבבי בנייה, כל אחד עם typecheck+lint+test ירוקים לפני שהתקדמתי הלאה:**

1. `bf7e254` — פרטי קשר חסרים (Place/Contact/Insurance) + jet_ski + Flight.legType נחשף (היה כתוב ביצירה, לעולם לא נקרא בחזרה — לא ב-shared-types, לא ב-Mock, לא ב-4 מקומות ב-Prisma) + LiveTimer גנרי חדש (מחובר לטיסות/מעבורות/צ'ק-אין-אאוט/החזרת-רכב/פעילות-מתוכננת) + Airport Timing calculator חדש. אגב כך התגלו ותוקנו 2 שדות נוספים שנכתבו אך אף פעם לא נקראו: `HotelStay.timezone`, `PlannedActivity.plannedTimezone`.
2. `32d5af6` — Rain Window (`computeRainWindows`, 5 בדיקות) ב-/today ו-/now.
3. `bfb0b99` — יתרת פתיחה לארנק כשלב ביצירת טיול (עד 3 מטבע+סכום), בורר קטגוריה ב-Nearby, כרטיס "מזג אוויר במיקום שלי עכשיו" (GPS אמיתי) ב-/today.
4. `95f387e` — חיפוש טווח-תאריכים עצמאי להוצאות/מסמכים בעמוד הטיול; `Trip.medicalNotes` + `DocumentType.passport_copy` (שדות חדשים למסך חירום).
5. `693a19e` — תגית "הבא" (טיימר חי) בכרטיס "התוכנית של היום" + כרטיס "קרוב אליי" חדש בדשבורד.
6. `2a1a495` — `restoreCarRental` (היה חסר כיחיד מ-7 סוגי-ישות עם Soft Delete) + Undo מלא.
7. `adf5159` — `Place.toggleDontReturn` — התגלה שזה לא היה רק מגבלת-גיבוי אלא פער אמיתי (0% שימוש באפליקציה החיה כולה).
8. `84ab4ad` — רכיבי Error State גנריים חדשים (`BlockedIntegrationState`/`PermissionDeniedState`), מוחלים על Mapbox panel + 4 מצבי Geolocation-denied.
9. `ac11b66` — Skip Link ("דלג לתוכן הראשי"), CSS-only.
10. `14ce304` — ייצוא CSV לכל סוגי ההזמנות + "ייצוא דוח מלא".

**תובנה חוזרת (כמו בסבבי P0-P3):** רוב הפערים היו "השדה כבר בסכימה, רק לא מחובר ל-UI/קריאה בחזרה" — לא תכנון סכימה מחדש. זה מאיץ מאוד את הקצב וממזער סיכון רגרסיה.

**מגבלה שמובאת בכנות:** בניגוד ל-P0-P3, שלב A **לא כלל בדיקה חיה בדפדפן** — אין כלי דפדפן/צילום מסך זמין בסביבת ה-Session הזו. אימות מבוסס typecheck strict + 322 בדיקות (ללא רגרסיה) + build מוצלח. מומלץ אימות ידני בדפדפן אמיתי לפני שנחשב ברמת ביטחון זהה לסבבים הקודמים.

**מספרים אחרי שלב A:** 66 IMPLEMENTED (+12), 38 PARTIAL (-10), 6 NOT IMPLEMENTED (-2), 4 BLOCKED (ללא שינוי מספרי). ר' `FEATURE_AUDIT.md` לפירוט המלא לכל שורה.

**נוצר גם:** דף "מפת דרכים" מפורסם (Artifact) לפי בקשת המשתמש — לינק קבוע שמציג את 6 שלבי A-F ואת מה שבעבודה עכשיו, מתעדכן באותו URL. (הלינק עצמו לא נשמר בקובץ הזה כי URLs של Artifacts הם per-conversation — המשתמש שמר אותו מהשיחה.)

## עדכון 2026-08-17 — P1 בעבודה (4 קבוצות ראשונות)

ממשיך אוטונומית מ-P0 (למעלה) ל-P1, לפי אותה הנחיה (בלי שירותים חיצוניים, typecheck+lint+test+build+בדיקה חיה+commit נפרד לכל קבוצה):

1. `d2c88f9` — Time Zones בפועל: `apps/web/lib/dates.ts` חדש (`Intl.DateTimeFormat` בלבד, בלי ספרייה) + שולב בתצוגת טיסות ב-3 מסכים (trip page, today, now).
2. `35dda62` — Show Proof of Payment (#45): כל הוצאה מציגה `<details>` עם רשימת התשלומים שלה (סכום/אמצעי/כרטיס אם אשראי) לצד המסמכים.
3. `9796fe9` — Wallet Reconciliation: `reconcileWallet` חדש ב-FinanceRepository (Mock+Prisma) + `WalletTxType.adjustment` חדש בסכימה + טופס "התאמת מזומן בפועל" בכל כרטיס ארנק.
4. `26e8ab6` — ארנק רב-מטבעי: שער יציג חי (בנק ישראל + Frankfurter כ-Fallback ל-THB וכו', לעולם לא שער מומצא) + שווי מוערך בש"ח לכל ארנק + סיכום תשלומי אשראי מקובץ לפי כרטיס. **תגלית תוך כדי:** התכנון המקורי (route נפרד `/trips/[tripId]/finances`) התברר כמיותר — הסקשן הקיים בעמוד הטיול כבר מרכיב הכל inline ותפקודי; שופר המסך הקיים במקום לבנות route כפול, ותוקנו 5 קישורים מתים שהצביעו לשם.
5. `4bbf805` — העדפות אפליקציה מלאות: `AppPreferences` חדש (מטבע בית/ניווט/מפה/יחידת טמפרטורה) ב-Context+localStorage (כמו Theme), UI ב-`/settings`, ו-**יחידת הטמפרטורה שולבה בפועל** (`components/temperature.tsx`) בכל 5 מקומות שמציגים טמפרטורה — לא הושארה כהעדפה מאוחסנת-ולא-בשימוש. מטבע ניווט/מפה עדיין לא משפיעים על התנהגות בפועל (דורשים Mapbox) — מתועד כמגבלה ידועה.
6. `faf92d3` — מערכת תקציב מלאה: `Trip.totalBudgetAmount`/`dailyBudgetAmount` + מודל חדש `BudgetCategoryLimit` (תמיד ב-₪, ר' הערה בסכימה). `apps/web/lib/budget.ts` ממיר הוצאות רב-מטבעיות דרך אותו `CurrencyRateProvider` חי שכבר משמש לארנקים — מטבע בלי שער זמין לא נכלל בסכום (לא מומצא ערך). Progress bar חי בעמוד הטיול + Budget Alert (מתקרב/חריגה, כולל+קטגוריה) ב-`gap-detection.ts` — **סוגר גם את ה-Budget Alert שנדחה בכוונה מ-P0**.
7. `056e5ce` — לוח שנה: `apps/web/lib/calendar.ts` חדש (buildCalendarEvents+buildMonthGridDates, 8 בדיקות) + `/trips/[tripId]/calendar` — תצוגת חודש עם ניווט קודם/הבא, אירועי מלון/טיסה/הסעה/תכנון עתידי, כל יום מקושר לסיכום היום הקיים (לא שוכפל).
8. `6064a28` — חיפוש גלובלי מורחב: `apps/web/lib/global-search.ts` חדש (searchAllEntities, 11 בדיקות) — /dashboard מחפש עכשיו טיולים+מקומות+אנשי קשר+מלונות+טיסות+הסעות+הוצאות+מסמכים (לא רק שמות טיולים), תוצאות מקובצות לפי סוג עם קישור ישיר ליעד.
9. `9468f58` — רשימת אריזה + צ'קליסט לפני הטיול: `ChecklistItem` חדש בסכימה (listType=packing/before_trip חולקים מבנה זהה) + `/trips/[tripId]/packing` — checkbox עם auto-submit (בלי כפתור "שמור"), רשימת אריזה מקובצת לפי קטגוריה עם התקדמות "X/Y".
10. `fcc39e7` — הרחבת מרכז מסמכים: `EntityDocumentSection` (הרכיב הגנרי הקיים) נוסף למקומות בטיול, לתכניות עתידיות (List View בלבד — Kanban נשאר קומפקטי בכוונה), ולכל Payment בודד בתוך "הצג הוכחת תשלום" — סוגר את 3 סוגי הישות שהיו חסרים מתוך 10.
11. `988202b` — הרחבת סיכום יום: כרטיס "מזומן/אשראי היום" (מקבץ Payments לפי `paymentAt`+`paymentMethod`, לא `expenseAt` — משקף מתי שולם בפועל) + כרטיס "עיסויים וטיפים היום" (ספירה+סכום לפי מטבע) ב-`days/[date]`.
12. `2ab73c9` — הרחבת סיכום סוף טיול (הפריט האחרון ב-P1): `TripGeographyRepository` חדש מחבר את `TripCountry`/`TripCity` שהיו קיימים בסכימה ב-0% שימוש; "מדינות וערים בטיול" בעמוד הטיול (pills עם הוספה/הסרה); דוח הטיול (`/trips/[tripId]/report`) קיבל כרטיסי מלונות/טיסות/עיסויים/ציר-זמן (האחרון משתמש חוזר ב-`buildCalendarEvents` מ-Calendar). **תמונות לא נכללו בכוונה** — דחוי ל-Trip Memories (P2), אין עדיין מנגנון מסמך ברמת-טיול כללי.

**P1 הושלם במלואו — 11/11 פריטים.** ראה `IMPLEMENTATION_GAPS.md` לפירוט המלא של כל commit. עברים ל-P2.

## עדכון 2026-08-17 — P2 סבב 1

`b71347c` — 4 פריטי P2 עצמאיים וממוקדים:
1. **Debit Card** — `PaymentMethod.debit_card` חדש; טופס תשלום מקשר גם אותו לכרטיס ספציפי (לא רק אשראי); "מזומן/כרטיס היום" ב-Day Summary מקבץ אשראי+חיוב יחד תחת "כרטיס" (סיכום כרטיסי האשראי הייעודי נשאר credit_card בלבד — עקבי עם השם שלו).
2. **Nearby range picker (`/now`)** — בורר 1/5/10/30/100 ק"מ במקום 30 קבוע.
3. **Rain-During-Activity Alert** — הפריט האחרון מתוך 15 סוגי Alert שהיה חסום; `detectRainDuringActivities` (weather-alerts.ts) + ב-`/today` כל תכנון עתידי היום עם מקום+שעה מקבל בדיקת גשם אמיתית (אותו דפוס "שעה קרובה ביותר בטווח 90 דק'" כמו Weather Timeline). **כעת 14/15 סוגי Alert עובדים — נשאר רק Refund Pending (החלטת scope מכוונת, לא באג).**
4. **Weather Cache** — תגלית תוך כדי: `OpenMeteoWeatherProvider` קרא ל-API בנפרד לכל אחד מ-current/hourly/daily לאותו מיקום, למרות ששלושתם מגיעים מאותה תגובה בפועל. עכשיו cache-per-instance עם TTL 10 דקות, משתף גם בקשות מקבילות (cache שומר Promise, לא רק תוצאה) — כשל לא נשמר בקאש.

נבדק: typecheck+lint+test+build ירוקים (139+116+8 בדיקות), `/now`/`/today`/עמוד טיול נבדקו חיים.

## עדכון 2026-08-17 — P2 סבב 2

`c0ab55c` — 2 תיקונים ממוקדים:
1. **Favorites (#74 PARTIAL→IMPLEMENTED)** — `Place.isFavorite` היה קיים בסכימה ומוצג עם כוכב, אבל לא ניתן היה לשנות אותו מה-UI בכלל מעבר לברירת המחדל `false` בזמן יצירה. `PlaceRepository.toggleFavorite` חדש (Mock+Prisma+2 בדיקות) + כפתור ★/☆ בכל שורת מקום + צ'יפ סינון "★ מועדפים (N)" ב-`/places`, משולב עם סינון הקטגוריה הקיים. **תובנה:** מכיוון ש-`Place` כבר גלובלי (לא פר-טיול), `/places?favorite=1` *הוא בפועל* מסך "כל המועדפים שלי" חוצה-הטיולים — לא היה צריך מסך נפרד.
2. **תיקון שדות פרי** — טופס יצירת הוצאה הציג את `itemName`/`quantity` (קיימים בסכימה+Repository לכל קטגוריה, לא רק shopping) רק כש-`category === "shopping"`, למרות ש-`DEDICATED_CATEGORIES` כוללת גם `fruit`. עכשיו שני התנאים מציגים את השדות (עם placeholder מותאם: "איזה פרי" לעומת "מה קניתי").

**במכוון לא נכלל בסבב הזה** (נשארים ב-P2): Repeat Visits (#75, לוגיקת "חזרתי לעיר" — תכנון גדול יותר), Personal Ratings על Hotel/Transport/Place/PlannedActivity (#73, דורש שינוי סכימה ב-4 מודלים).

נבדק חי: `/places?favorite=1` מציג רק את המלון המסומן כמועדף בנתוני הדמו.

## עדכון 2026-08-17 — P2 סבב 3

`2e93f7e` — Units + Accessibility, שניהם הרחבה של תשתית Preferences קיימת (לא מערכת חדשה):

1. **Units** — `AppPreferences` (מ-P1) הורחב עם `defaultDistanceUnit`/`defaultTimeFormat`. `lib/distance-format.ts`+`components/distance.tsx` ו-`lib/time-format.ts`+`components/time.tsx` (אותו דפוס בדיוק כמו `Temperature` מ-P1) — הומרו בפועל ב-RouteStop (`days/[date]`, גם מרחק וגם שעות הגעה/יציאה) וב-`NearbyPlaces` (מרחק). 8 בדיקות חדשות.
2. **Accessibility** — `ThemePrefs` (מ-Theme הקיים, לא Preferences) הורחב עם `contrast`/`textSize`, מוחל דרך אותה תשתית `data-*`+CSS כמו brightness/density/rounded/animations שכבר קיימת. High Contrast מחזק `--color-text-muted`/`--color-border` בתוך הפלטה הקיימת (לא סכימת צבעים נפרדת). Large Text מגדיל `font-size` על `html` ב-118% — כיוון שכל סקאלת הריווח/הטיפוגרפיה בנויה על `rem` (כולל בסגנונות inline), זה מגדיל את כל האפליקציה בעקביות, לא רק טקסט בודד.

נבדק חי: `/settings` מציג את 4 הבקרים החדשים, ה-CSS מוגש בפועל בשרת ה-dev.

## עדכון 2026-08-17 — P2 סבב 4

`0bd60b7` — Trash מרוכז + Restore לכל הישויות (#96+#97, הפריט המהותי האחרון שנותר ב-P2 מהרשימה המקורית):

- `restore()`/`restoreX()` חדש ב-4 ה-Repository (Place/PlannedActivity/Booking [4 סוגים: HotelStay/Flight/TransportBooking/Insurance]/Finance [Expense]) — Mock+Prisma, 4 בדיקות round-trip חדשות. **תובנה טכנית:** ב-Prisma, HotelStay/Flight/TransportBooking/Insurance "מתבגרים" מ-Booking גנרי (01_architecture_v2.md) — ה-`deletedAt` שלהם חי בפועל על ה-Booking ההורה, לא על הרשומה עצמה, אז restore מעדכן את ה-Booking (אותו דפוס בדיוק כמו softDelete הקיים לאותן ישויות).
- `includeDeleted?: boolean` נוסף לכל מתודות ה-list הרלוונטיות (ברירת מחדל `false` — לא משנה שום התנהגות קיימת).
- `/trash` (חדש) — מרכז פריטים מחוקים מכל הטיולים הפעילים של המשתמש (כולל Place הגלובלי) בסקשנים לפי סוג, עם כפתור שחזור. טיול שנמחק לגמרי לא נסרק (מוסבר בעמוד — יש לשחזר את הטיול קודם, ה-UI לזה כבר קיים). קישור "פח" נוסף לתפריט הצד.

נבדק: typecheck+lint+test+build ירוקים (145+124+8 בדיקות), `/trash` נבדק חי (מצב ריק כן).

## עדכון 2026-08-17 — החלטת משתמש: Component Library נדחה + P2 סבב 5

נשאלה שאלה מהותית: הפריט הבא ב-P2 היה "Component Library" (14 רכיבי UI: Modal/Drawer/Tooltip/Toast/FilterChip/PageHeader/MapCard/WalletCard/ExpenseCard/BookingCard) — נוגע ישירות בעיצוב, וארנון ציין שיעביר "חבילת UI סופית" בנפרד. **החלטה: דלג על Component Library, המשך בשאר P2** (הלא-עיצוביים). מתועד כדי שסבב עתידי לא יבנה את זה בטעות בלי לתאם קודם.

`a032f97` — סבב 5: Multiple Quotes לתחבורה. `TransportQuote` היה קיים ב-schema.prisma בלבד (0% ב-shared-types/Repository/UI). נבנה: shared-types חדש, 4 מתודות ב-BookingRepository (Mock+Prisma+3 בדיקות), `TransportQuotesSection` — רשימת השוואה ממוינת זול-קודם עם בחירה ידנית ("איזו הצעה בחרתי", לא המרה אוטומטית להזמנה — זה נשאר שלב עתידי נפרד), משולבת תחת סקשן "תחבורה" בעמוד הטיול. נבדק חי: 2 הצעות דמו (Grab ₺850, Airport Transfer ₺1200) מוצגות נכון.

`b0f4060` — סבב 6: Global Places Library stats. `apps/web/lib/place-stats.ts` חדש (computePlaceStats, 6 בדיקות) — ביקור=TripPlace.status="visited" (ייחודי לפי trip+place, כל טיול נספר פעם אחת), הוצאה=Expense.placeId מפורש בלבד (לא ניחוש גיאוגרפי). `/places` סורק את כל טיולי המשתמש (Place גלובלי, אותו דפוס כמו global-search.ts/trash) ומציג "ביקרתי X פעמים · הוצאתי Y" לכל מקום. נתוני הדמו הקיימים לא מניבים תוצאה לא-ריקה כרגע (אין Place עם TripPlace/Expense מקושרים בפועל בזרעי הדמו הנפרדים) — הלוגיקה מכוסה במלואה ב-6 הבדיקות.

`22e5e06` — סבב 7: Tourist Tax + דוח צבירה לעיסויים/פירות. **החלטה מכוונת:** לא נבנה מודל ייעודי ל-Tourist Tax — קטגוריית הוצאה היא טקסט חופשי בכוונה (DECISIONS.md, 2026-08-15), אז מודל נפרד היה סותר החלטה קיימת. במקום זה "tourist_tax" נוסף כקטגוריה מוצעת — Expense הכללי כבר תומך בכל מה שצריך (סכום/מטבע/מקום/תאריך) וזורם אוטומטית לתקציב/דוחות. מסומן PARTIAL בכוונה ב-FEATURE_AUDIT (לא "ייעודי" כמו שהתבקש במקור). בנוסף: כרטיס "עיסויים" בדוח הטיול קיבל ממוצע-לעיסוי (רק כשמטבע אחיד — לא ממוצע מטעה), וכרטיס "פירות" חדש (ספירה+סכום+כמות).

`72699b6` — סבב 8: 3 קיצורים חדשים בתפריט ההוספה המהירה (Currency-Exchange/Tip/Massage, #112, 5/9→8/9). הפונקציונליות כבר הייתה קיימת (CurrencyExchangeForm, ExpenseCreateForm עם category=tip/massage) — רק לא היה קיצור ישיר. `BookingGroup` (עמוד הטיול) מקבל `id` אופציונלי; `components/open-details-from-hash.tsx` חדש פותח `<details>` סגור בהתאם ל-hash בטעינה (גלילת עוגן טבעית לא פותחת `<details>`). Scan-Receipt נשאר החסר היחיד — חסום ע"י OCR (Claude API key).

`819f7c5` — סבב 9: מנגנון Undo (#98, NOT IMPLEMENTED→PARTIAL). `components/toast-provider.tsx` חדש — Context+Hook כללי (`useToast`) מוצג ב-Root Layout לצד ThemeProvider/PreferencesProvider; `components/delete-with-undo-button.tsx` חדש — מחליף `confirm()`+`<form action>` הישן: מוחק מיד, `router.refresh()`, ומציג טוסט "X הוסר. בטל?" ל-6 שניות שקורא לפעולת `restoreX` הקיימת. מחובר ל-7 נקודות מחיקה שכבר יש להן `restore()` מסבב Trash (Place/PlannedActivity/Expense/HotelStay/Flight/TransportBooking/Insurance). **החלטות scope מכוונות ומתועדות:** CarRental לא כלול (אין לו `restore()`), Trip נשאר עם `confirm()` הישן (ה-action מבצע `redirect()` מיידי ל-`/trips` אחרי המחיקה — אין הזדמנות להציג טוסט על אותו עמוד לפני הניווט; שחזור עדיין זמין דרך `/trash`), Document/Contact ללא Undo (אין להם `restore()` בכלל — לא נבנה מנגנון חדש רק בשביל זה, מחוץ ל-scope הסבב).

`1dc4459` — סבב 10: הרחבת Personal Ratings (#73, PARTIAL→IMPLEMENTED). `personalRating` (`Int?`, 1-5) נוסף ל-4 מודלים ב-`schema.prisma`: Place, PlannedActivity, HotelStay, TransportBooking. **הבדל מכוון מ-Expense:** אצל Expense הדירוג נקבע בזמן היצירה (הגיוני — הוצאה נרשמת אחרי שכבר שילמו/חוו את זה), אבל את 4 הישויות האלה בונים בדרך כלל *לפני* החוויה בפועל (מזמינים מלון/טיסה/פעילות מראש) — אז דירוג-רק-ביצירה היה חסר תועלת ולא-כן ("איך אני יודע כמה כוכבים למלון שעוד לא ישנתי בו"). לכן לכל ישות נוספה פעולת עדכון צרה (`setPersonalRating`/`updatePersonalRating`) שמאפשרת לדרג בכל שלב אחרי היצירה — מקבילה בדיוק לדפוס `toggleFavorite`/`updateStatus` הקיימים בקוד (עדכון שדה יחיד, לא Update גנרי לכל השדות). רכיב UI חדש וגנרי `components/personal-rating-select.tsx` — `<select>` 1-5 כוכבים, מגיש מיידית עם `router.refresh()` בשינוי (בלי טופס נפרד) — מחובר ב-4 מסכי תצוגה: `/places`, `planned-activities-view`, ורשימות המלונות/תחבורה בעמוד הטיול. Mock+Prisma עודכנו יחד (Prisma לא נבדק חי, כרגיל), 8 בדיקות round-trip חדשות (set+clear+not-found × 4 ישויות). Expense עצמו (מגבלת 5 הקטגוריות הקיימת) לא נגע — מחוץ ל-scope הסבב.

`af5261d` — סבב 11: Repeat Visits (#75, NOT IMPLEMENTED→IMPLEMENTED). `lib/repeat-visits.ts` חדש (7 בדיקות) — `computeRepeatVisitSuggestions`: משווה את הערים של המקומות שכבר מקושרים לטיול הנוכחי (TripPlace) מול מקומות שסומנו `visited`/`favorite` בטיולים קודמים (לפי `startDate` מוקדם יותר) לאותה עיר. עיר נלקחת רק מ-`Place.city` המפורש — לא ניחוש גיאוגרפי/קירוב מיקום. מכבד את שני איתותי "לא לחזור" הקיימים במודל בו-זמנית: `Place.dontReturn` (סימון גלובלי על המקום) ו-`TripPlaceStatus="dont_return"` (סימון פר-טיול, פשוט לא נכלל ברשימת הסטטוסים הרלוונטיים). מקומות שכבר מקושרים לטיול הנוכחי לא מוצעים שוב (נבדק לפי `placeId`, לא רק שם). מיון: דירוג אישי גבוה קודם, אחר כך מועדפים. UI חדש: `repeat-visits-section.tsx` — כרטיס "חזרת ל-X?" בעמוד הטיול (רק כשיש הצעות בפועל, לא ריק כברירת מחדל), עם כפתור "הוסף לטיול" חד-קליק לכל הצעה — פעולה חדשה `quickLinkPlaceToTripAction` (`trip-places/actions.ts`) שמעטפת את ה-`linkPlaceToTrip` הקיים (upsert קיים כבר ברמת ה-Repository) בלי צורך בטופס `useActionState`. **מגבלה מתועדת:** מבוסס `Place` בלבד — לא סורק שמות `HotelStay` ישירות, כי אין קישור `Place↔HotelStay` בסכימה (Booking כן מחזיק `placeId` אופציונלי, אבל HotelStay עצמו לא) — מגבלה דומה באופיה להיקף המתועד של Recommendations הכללי.

`5089b98` — סבב 12: Trip Memories (#72, NOT IMPLEMENTED→IMPLEMENTED). הדרישה המקורית היא "תמונות/הערות/ציר-זמן" — בבדיקה התברר ש-2 מתוך 3 כבר היו מכוסים במלואם (`Trip.notes` עם UI עריכה מלא ב-`TripEditForm`, וציר-זמן דרך `buildCalendarEvents` שכבר מוצג בדוח הטיול מסבב P1). הפער האמיתי היה רק תמונות. **החלטת ארכיטקטורה:** לא נבנה מודל `Photo` נפרד — נוסף `"trip"` ל-`DocumentEntityType` הקיים (ב-`schema.prisma` וב-`enums.ts` יחד, לפי הכלל של AGENTS.md), כי מנגנון ה-`Document` הקיים כבר עושה בדיוק את מה שצריך: `uploadDocumentAction` (ב-`documents/actions.ts`, נבנה בסבב מוקדם יותר) קורא את הקובץ שהועלה ל-Buffer וממיר ל-`data:` URI אמיתי (base64, לא כתובת מזויפת ולא placeholder) — בדיוק אותה תשתית ששימשה עד עכשיו לקבלות/אישורי הזמנה. `trip-memories-gallery.tsx` חדש הוא גרסה ויזואלית של `EntityDocumentSection` הקיים: גריד thumbnails ריבועי (`aspect-ratio: 1/1`, `object-fit: cover`) במקום רשימת קישורי 📎, כי "זכרון מהטיול" הוא חוויה ויזואלית ולא קובץ מצורף. טופס ההעלאה מקבע `documentType="image"` (hidden input) ו-`accept="image/*"` בלבד. סקשן חדש "זכרונות מהטיול" נוסף בעמוד הטיול, ליד "מדינות וערים". מגבלת הגודל הקיימת (3MB, בגלל שמירה בזיכרון-Mock ולא Cloud Storage אמיתי) חלה גם כאן.

**החלטת המשתמש (2026-08-17):** לאחר סבב 12, Localization מלא (עברית+אנגלית) היה הפריט האחרון הלא-חסום שנותר ב-P2. הועלה למשתמש במפורש (AskUserQuestion) כי ההיקף שונה מהותית משאר הפריטים — מאות מחרוזות עבריות קשיחות בעשרות קבצים בכל שלושת האזורים באפליקציה, דורש בחירת ספריית i18n + אסטרטגיית מעבר שפה + מסך/העדפת שפה חדשה, ומסתכן בהתנגשות עם "חבילת ה-UI הסופית" שתגיע בהמשך. **המשתמש בחר "דלג בינתיים (מומלץ)"** — Localization נדחה בכוונה, באותו אופן כמו Component Library קודם לכן. עם זה, כל שאר ה-P2 שלא תלוי בשירות חיצוני הושלם: 12 סבבים (Debit Card, Nearby range picker, Rain-During-Activity Alert, Weather Cache, Favorites toggle, Units, Accessibility, Trash+Restore, Multiple Quotes, Global Places stats, Tourist Tax, Massage/Fruit reports, Mobile Add Menu, Undo mechanism, Personal Ratings מורחב, Repeat Visits, Trip Memories). נשארו רק שני פריטים נדחים-בכוונה (Component Library, Localization) ו-P3 (חסום שירותים חיצוניים).

`07d378e` — P3 סבב 13: Share + WhatsApp Sharing (#85, #86, NOT IMPLEMENTED→IMPLEMENTED). לאחר שהתברר שאשכול P3 "עתידי, תלוי שירות חיצוני" הכיל פריטים שלא באמת חסומים, המשתמש נשאל במפורש (AskUserQuestion) ואישר להמשיך לבנות 4 מהם — זהו הראשון. `lib/trip-share-text.ts` חדש (7 בדיקות) — `buildTripShareText` מרכיב תקציר טקסט קריא (שם/תאריכים/מדינות-ערים/מלונות/טיסות/תחבורה) מהנתונים שכבר מחושבים בדוח הטיול (`report/page.tsx`) — לא בונה שום נתון חדש. **החלטה מכוונת:** משתפים טקסט, לא URL לצפייה — כי אין עדיין מסך צפייה ציבורי לא-מאומת (#88 Share Security תלוי שיהיה קודם מה לאבטח, ונשאר NOT IMPLEMENTED בכוונה). `components/share-button.tsx` — `navigator.share()` Web API של הדפדפן, עם fallback אמיתי להעתקה ללוח (`navigator.clipboard`, טוסט הצלחה/כישלון דרך `ToastProvider` הקיים מסבב P2-9) — לא מסתיר את הכפתור בדפדפנים בלי תמיכה. `components/whatsapp-share-link.tsx` — קישור `https://wa.me/?text=...` פשוט, תמיד זמין (לא תלוי ב-`navigator.share`). שני הכפתורים מחוברים בראש `/trips/[tripId]/report`, ליד הכותרת.

`f83f0d7` — P3 סבב 14: Report Builder (#89, NOT IMPLEMENTED→IMPLEMENTED). דוח הטיול הקבוע ב-`report/page.tsx` הפך למוגדר-אישית דרך query params — אותה שיטה בדיוק כמו סינון from/to/city/country שכבר היה קיים בעמוד, לא נבנה מסך נפרד. שלושה חלקים תואמים בדיוק לניסוח האודיט ("Sections/Trip/מטבעות"): (1) **Sections** — 14 checkboxes בתוך `<details>` מתקפל ("בונה דוח"), `name="sections"` (ערך מרובה, HTML native), שולטות אילו מ-14 ה-`<Card>` הקיימים מוצגים; ברירת מחדל **קריטית**: כש-`sections` לא מופיע ב-URL בכלל (כלומר כל מי שכבר סימן bookmark לעמוד הזה) — כל 14 הסקשנים מוצגים, זהה ל-100% להתנהגות הקודמת, כדי לא לשבור התנהגות קיימת. (2) **Trip** — `trip-picker.tsx` חדש, קומפוננטת client קטנה עם `<select onChange>` שמנווט ל-`/trips/{tripId}/report` של טיול אחר (`router.push`) — נותן יכולת "בחירת טיול" מתוך מסך הדוח עצמו בלי לבנות ארכיטקטורת cross-trip נפרדת. (3) **מטבעות** — פילטר `currency` חדש (מוצג רק כשיש יותר ממטבע אחד בפועל בטיול), מסנן את רשימת ה-`expenses` המשמשת את הסקשנים הכספיים בלבד (עלות כוללת/ממוצע-יומי/קטגוריה/יומי/טיפים/עיסויים/פירות) — **החלטה מכוונת:** לא מסנן מלונות/טיסות/ציר-זמן/ארנקים, כי אלה לא "הוצאה במטבע X" באותו מובן, וסינון שלהם היה מטעה.

`be7936d` — P3 סבב 15 (האחרון מ-4 שאושרו): Backup Architecture (#90, NOT IMPLEMENTED→PARTIAL). `packages/shared-types/src/backup.ts` חדש — `backupFileSchema` מכסה 25 סוגי ישות () עם `formatVersion` לזיהוי גרסת מבנה עתידית. `lib/backup/export-backup.ts` — קורא `list()`/`includeDeleted:true` של כל Repository לכל טיול, מרכיב JSON אחד. `lib/backup/restore-backup.ts` — **החלק המורכב**: `create()` בכל Repository מייצר UUID חדש (לא מקבל id קבוע), אז שחזור דורש מיפוי id ישן→חדש בזמן ריצה לכל מפתח זר, ב-21 שלבים בסדר תלות נכון (Trip→Place→PaymentCard→Contact→TripCountry→TripCity→TripPlace→**Wallet (מוקדם בכוונה)**→5 סוגי Booking→BookingBenefit→TransportQuote→PlannedActivity→Expense→Payment→CurrencyExchange→Refund→Deposit→BudgetCategoryLimit→**תיאום ארנקים סופי**→Document→ChecklistItem). **נקודת התכנון הקריטית — ארנקים:** Wallet משוחזר מוקדם דרך `topUpWallet(initialAmount)` (לפני תשלומים/המרות/פיקדונות, כי אלה דורשים ארנק קיים כדי שהתאמת היתרה תעבוד — נצפה גם בלוג הבדיקות הקיים: "cash payment recorded without a matching wallet"), ואז **אחרי** שכל הפעולות המשפיעות על יתרה שוחזרו (שמשפיעות עליה כתוצר-לוואי, לא בול-מדויק כמו במקור), נקראת `reconcileWallet({actualBalance: wallet.currentBalance})` על כל ארנק — זה מבטיח שהיתרה הסופית מדויקת ב-100% ללא תלות בסדר-הביניים. **4 מגבלות מתועדות בכוונה, מדווחות ב-`RestoreSummary.skipped` ולא נבלעות בשקט:** (1) `WalletTransaction` — אין `create()` ישיר ב-Repository (רק תוצר-לוואי של `topUpWallet`/`createPayment`/וכו') אז תנועות בודדות לא משוחזרות כרשומות נפרדות, רק ההשפעה המצטברת על היתרה; (2) `TransportQuote.transportBookingId` — אין setter חשוף (`toggleTransportQuoteSelected` מטפל רק ב-`isSelected`); (3) `Place.dontReturn` — אין toggle חשוף ב-`PlaceRepository`; (4) `Document` עם `entityType="other"` או `"booking"` — אין מיפוי id יחיד-משמעי (other לא מזוהה, booking גנרי מדי). UI: `components/download-backup-button.tsx` חדש (מקביל בדיוק ל-`ExportCsvButton` הקיים — Blob+`URL.createObjectURL`), `settings/restore-backup-form.tsx` חדש (`useActionState`, טופס העלאת קובץ, מציג סיכום `restoredCounts`+`skipped` אחרי שחזור), `settings/actions.ts` חדש (`restoreBackupAction` — מגבלת 20MB, `backupFileSchema.safeParse` לפני קריאה ל-`restoreBackup`). **הרחבה קריטית:** `documentEntityTypeSchema` כבר כלל `"trip"` מסבב 12 (Trip Memories) — נוצל ישירות כאן בלי שינוי נוסף. **אימות:** typecheck+lint+test(156+8 קיימים, לא נוספו חדשים)+build כולם ירוקים; **לא בוצע אימות חי בדפדפן** — שרת ה-dev שרץ על המכונה בזמן הבדיקה התברר כשרת של הפרויקט limme (לא טיולים, פורט 3000 חופף), ולא הופעל שרת ייעודי חדש; **לא נכתב unit test אוטומטי** — `export-backup.ts`/`restore-backup.ts` תלויים ב-Singletons של `getXRepository()` (לא ב-instances מבודדים כמו כל שאר בדיקות ה-repository בפרויקט שמייצרות `new MockXRepository()` טרי ב-`beforeEach`), אז בדיקה מבודדת הייתה דורשת שינוי ארכיטקטוני נפרד — הוחלט שלא לעשות זאת עכשיו, ולסמוך על typecheck ההדוק (כל שדה עבר טיפוס Zod-inferred מפורש) + בדיקה ידנית עתידית.

## עדכון 2026-08-16 (סבב 3) — בניית P0 מלאה (9 קבוצות עבודה)

המשתמש אישר להתחיל לבנות לפי סדר P0→P1→P2 לפי `IMPLEMENTATION_GAPS.md`, **בלי לחבר שירותים חיצוניים** — כל מה שבנוי בעתיד עם שירות חיצוני (Mapbox/Weather-API-חדש/OCR) צריך להיבנות עם UI+לוגיקה+interfaces+adapters+demo data מוכנים מראש, לא Placeholder. עבד באופן עצמאי דרך 9 קבוצות, כל אחת עם typecheck+lint+test+build+בדיקה חיה מול שרת ה-dev+commit נפרד:

1. `a60061f` — שדות משניים להזמנות (floor/view/breakfast/terminal/baggage/ETA/tolls) + Booking Benefits מלא.
2. `4b16a34` — מסמכים ל-5 סוגי ישות (לא רק Expense) + מרכז מסמכים עם סינון.
3. `c944b9b` — סיכום יום ב-days/[date].
4. `f7f78c8` — Weather Alerts + Weather Timeline + מזג אוויר ביעד הבא (כולם על Open-Meteo הקיים).
5. `4f4c19a` — מזג אוויר + Rain Alert ב-/now.
6. `d46bc60` — 5/8 סוגי Alert חסרים + checkInTime/checkOutTime שהיו קיימים בסכימה בלי UI.
7. `6676871` — כפתור "המיקום שלי" ב-/map (Leaflet, Geolocation חינמי).
8. `62a14e6` — מסך חירום חדש (/emergency).
9. `25b3978` — Print/PDF דרך הדפדפן (בלי ספרייה חדשה) + ייצוא CSV.

**תובנה חוזרת לאורך הסבב:** במקרים רבים ה-Prisma schema כבר הכיל את השדה (floor/view/checkOutTime/lat/lng וכו') — הפער היה רק בחיבור shared-types→Repository→UI, לא בתכנון סכימה מחדש. זה האיץ משמעותית את הקצב.

**מספרים:** `FEATURE_AUDIT.md` עודכן — Implemented עלה מ-30 ל-36, Not Implemented ירד מ-36 ל-24 (מתוך 114). `IMPLEMENTATION_GAPS.md` עודכן עם רשימת P1/P2/P3 מחודשת.

**המשימה הנוכחית:** ממשיך אוטונומית ל-P1 (11 פריטים ב-IMPLEMENTATION_GAPS.md: Time Zones, Budget, Calendar, Wallet Reconciliation, Global Search, Packing List+Checklist, Show Proof of Payment, Preferences, Day/End-Trip-Summary extensions, Document Center extension) ואז P2 — לפי אותה הנחיה (ללא שירותים חיצוניים, typecheck/lint/test/build אחרי כל קבוצה, docs מעודכנים שוטף).

## עדכון 2026-08-16 (סבב 2) — Audit מדויק מול רשימת בדיקה מפורשת בת 114 פריטים

המשתמש ראה את תוצאות הסבב הראשון וביקש סבב שני, מדויק יותר: לעבור על רשימת בדיקה מפורשת שהוא נתן (114 פריטים בדיוק, לא מסמך 156 הסעיפים המקורי) אחד-אחד, עם מספרים **מדויקים** (לא "כ-") ופירוט "מה בדיוק חסר" לכל פריט P0. בוצע: `FEATURE_AUDIT.md` ו-`IMPLEMENTATION_GAPS.md` **נכתבו מחדש במלואם** במבנה שתואם 1:1 לרשימה של המשתמש (הסבב הראשון עדיין קיים בהיסטוריית Git, commit `e6cd8d8`).

**מספרים מדויקים (114 פריטים בסה"כ):** Implemented=30, Partial=43, UI Only=1, Mock Only=0, Not Implemented=36, Blocked=4.

**שאלת "Remote Control initialization failed" שהמשתמש שאל:** נבדק — `grep` מלא על כל קוד המקור של אפליקציית הטיולים (`apps/web`, `packages/*`) לא מצא שום אזכור ל-"Remote Control"/"RemoteControl" — זו **לא תקלה בקוד האפליקציה עצמה**. זו כנראה הודעה מרובד הכלים של Claude Code עצמו (יכולת שליטת-דפדפן-מרחוק שכבר תועדה בשיחה הזו כלא-זמינה בסביבה הזו — "אין כלי דפדפן/צילום מסך"). **הוכחה שזה לא חוסם עבודה מקומית:** לאורך הסבב הזה בלבד הרצתי בהצלחה, ברצף, ללא הפרעה: קריאה/עריכה/כתיבה של קבצים, `git commit` (פעמיים), `npm run typecheck/lint/test/build`, ו-`curl` מול שרת ה-dev הרץ. כל אלו עבדו נורמלית. המסקנה: השגיאה חוסמת רק יכולת שאיני משתמש בה בכלל בזרימת העבודה הזו (שליטת דפדפן/צילום מסך), **לא** את היכולת לפתח/לבנות/להריץ קוד.

## עדכון 2026-08-16 (מוקדם) — Audit מלא מול מסמך דרישות מאוחד בן 156 סעיפים + עבודה נוספת מהיום

מאז הרשומה למטה (שנכתבה בסוף יום 2026-08-16 מוקדם יותר) קרו שני דברים משמעותיים נוספים:

1. **ארנק רב-מטבעי — שער יציג חי (commit `26e8ab6`)**: נוסף `CurrencyRateProvider` (בנק ישראל + Frankfurter, לעולם לא ממציא שער), `listPaymentsByTrip` + סיכום כרטיסי אשראי, ותוקנו 5 קישורים מתים ל-`/trips/[tripId]/finances` (route שמעולם לא היה קיים — התגלה תוך כדי שסקשן "כספים" כבר מרכיב הכל inline בעמוד הטיול הראשי, בדיוק כמו bookings/documents). פירוט מלא ב-DECISIONS.md.

2. **Audit מלא מול מסמך דרישות מאוחד (156 סעיפים) שהמשתמש סיפק** — ראה `FEATURE_AUDIT.md` (טבלת סטטוס לכל דרישה) ו-`IMPLEMENTATION_GAPS.md` (רשימת פערים מתועדפת P0-P3). בוצע ע"י 4 סוכני חקירה מקבילים (read-only) + בדיקה ישירה שלי (typecheck/lint/test/build ירוקים, 13 routes נבדקו חי עם curl, 170/170 בדיקות). **תוצאה מספרית:** מתוך כ-150 דרישות פרטניות — כ-30 IMPLEMENTED, כ-70 PARTIAL, כ-4 UI ONLY, כ-40 NOT IMPLEMENTED, 3 BLOCKED BY EXTERNAL SERVICE (Mapbox Token, Claude API key ל-OCR, Supabase). **שלוש קטגוריות שלמות כמעט ולא קיימות:** Export/Print/Share/Backup (0%), Calendar/Timers-מאוחדים (מפוזר), Localization/Units/Preferences-מלאות/Accessibility (רק Theme קיים).

**חשוב — סטטוס ה-Audit הזה הוא "דיווח בלבד, לא תחילת בנייה":** המשתמש ביקש במפורש "אל תתחיל עדיין גל פיתוח גדול בלי אישור שלי" ו"קודם בצע Audit מלא, הצג לי את התוצאות, ועצור". **זה עוקף באופן זמני** את ה"הנחיה העומדת" הישנה למטה ("להמשיך את כל הפרויקט עד הסוף ברצף, בלי לעצור לשאלות") — ההנחיה הישנה תקפה שוב ברגע שהמשתמש יאשר איזה אשכול פערים לתקוף. **הפעולה הבאה בפועל: להמתין לתשובת המשתמש** על סדר העדיפויות מתוך `IMPLEMENTATION_GAPS.md`, לא להתחיל לבנות שום דבר מהרשימה בלי אישור מפורש לכל אשכול.

## המשימה הנוכחית (ברגע כתיבת הקובץ הזה — היסטורי, מלפני ה-Audit)
**כל 5 השלבים המקוריים לפי 01_architecture_v2.md "נגעו" עכשיו** (1=ליבה, 2=Planned Activity/Route/Gap Detection, 3=מפה, 3.5=Weather, 4=Documents, 5=Offline) — חלקם הושלמו במלואם, חלקם בהיקף מוגבל בכוונה עם סייגים כנים במקום זיוף:
- שלבים 1-3.5: **הושלמו במלואם.**
- שלב 4 (Documents): קבלות על הוצאות עובד מקצה לקצה (נבדק חי עם PNG אמיתי). חסום אמיתית (לא רק "יבחר ספק בזמנו" כמו מפה/מזג אוויר) — Storage אמיתי + Claude API key ל-OCR.
- שלב 5 (Offline): PWA shell caching + באנר "לא מקוון" עובד (נבדק חי). סנכרון דו-כיווני אמיתי נדחה בכוונה — דורש שינוי ארכיטקטוני (כל האפליקציה על SSR+Server Actions), לא בוצע בלי אישור מפורש.

**שני החסמים האמיתיים (Storage+OCR, וההחלטה על ארכיטקטורת Offline) נרשמים לרשימת השאלות לסוף, לפי בקשת המשתמש.**

**Audit Log ו-Soft Delete (Expense+4 Booking types) הושלמו** (ראה סעיפים 25-26 למעלה). Soft Delete עכשיו מכסה: Trip, Place, PlannedActivity, HotelStay, Flight, TransportBooking, Insurance, Expense. **לא מכוסה בכוונה: Payment** (דורש עיצוב נפרד סביב תיקון יתרת ארנק, ראה DECISIONS.md).

**היסטוריית תנועות ארנק + Deposit (פיקדון) הושלמו** (ראה סעיף 27 למעלה) — פינה אחרונה שנותרה פתוחה בתחום הכספים.

**PaymentCard הושלם** (ראה סעיף 28 למעלה). תוך כדי, התגלה שסעיף "5ד. מעבורות/רכבת/אוטובוס" ב-PROJECT_REQUIREMENTS.md היה מיושן — התברר שהקוד כבר תומך בכל 7 ה-TransportMode-ים; תוקן ישירות בלי צורך בעבודה נוספת.

**StatusHistory + דוח "תכנון מול ביצוע" הושלמו** (ראה סעיף 29 למעלה).

**שעות פתיחה + "פתוח עכשיו" הושלם** (ראה סעיף 30 למעלה).

**סינון טיולים לפי טווח תאריכים הושלם** (ראה סעיף 31 למעלה).

**תחזית שעתית/יומית במסך "היום שלי" הושלמה** (ראה סעיף 32 למעלה).

**השכרת רכב/אופנוע (CarRental) הושלמה** (ראה סעיף 33 למעלה) — כולל טבלה חדשה ב-schema.prisma ו-enum חדש RentalVehicleType.

**המרת Planned Activity ל-Booking אמיתי הושלמה** (ראה סעיף 34 למעלה) — הפריט הגדול האחרון שהיה פתוח מתחילת הפרויקט, כולל תיקון פער אדריכלי (חשיפת Booking.id אמיתי) שהתגלה תוך כדי העבודה. **כל הפריטים הגדולים מה-01_architecture_v2.md הושלמו.**

**"מקומות קרובים אליי" הושלם** (ראה סעיף 35 למעלה) — הפריט האחרון שהיה מסומן "בקרוב" במסך עכשיו.

**דוח הטיול שודרג עם סינון תאריכים וגרפים** (ראה סעיף 36 למעלה).

**קנבן גרירה ל-Planned Activity הושלם** (ראה סעיף 37 למעלה).

**חישוב מרחק/זמן נסיעה אוטומטי במסלול (OSRM) הושלם** (ראה סעיף 38 למעלה) — נבחר ספק חיצוני חינמי/בלי מפתח (router.project-osrm.org, שרת Demo ציבורי, מתועד בכנות כלא-SLA-לפרודקשן ב-DECISIONS.md), באותו דפוס בדיוק כמו Leaflet+OSM ו-Open-Meteo.

**התראות דפדפן מקומיות הושלמו** (ראה סעיף 39 למעלה) — כיסוי חלקי בכוונה (2 מתוך 10 סוגי אירוע, רק אלה עם timestamp אמיתי בנתונים) ו-Notification API רגיל במקום Web Push אמיתי, מתועד בכנות ב-UI וב-DECISIONS.md מדוע (תשתית VAPID+Service Worker+שרת לא מתאימה למבנה הנוכחי, וגם נתקלת במגבלת ה-Service Worker הידועה בסביבת הבדיקה). נבדק חי מקצה לקצה כולל מסלול ה-permission "granted" (monkey-patch, כמו ל-Geolocation) עם טיסת בדיקה אמיתית.

**דוח הטיול שודרג עם סינון עיר/מדינה + נוסף עמוד השוואה בין טיולים** (ראה סעיף 40 למעלה). **זה היה הפריט הגדול-משמעותי האחרון שלא תלוי בשני החסמים החיצוניים — כל מה שבשליטתי מתוך 01_architecture_v2.md ו-PROJECT_REQUIREMENTS.md הושלם או תועד בכוונה כ-Not Started עם הסבר מדוע (Web Push אמיתי, 8 סוגי התראה נוספים).**

**ההנחיה העומדת של המשתמש היא להמשיך את כל הפרויקט עד הסוף ברצף, בלי לעצור לשאלות — אבל בשלב הזה אין עוד עבודה משמעותית שבשליטתי לבצע בלי המשתמש.** שני החסמים האמיתיים היחידים שנותרו הם חיצוניים ולא תלויים בי: **Supabase חי** (RLS+Auth+DB אמיתיים) ו**Storage+Claude API key** ל-OCR מסמכים — שניהם ברשימת השאלות למשתמש בסוף, כפי שסוכם מתחילת הפרויקט. זה הרגע לעצור ולשאול.

## הפעולה הראשונה שצריך לבצע לאחר Compact/Session חדש (מעודכן — 2026-08-17)

**כל התוכן מעל השורה הזו בקובץ הוא היסטורי** (תיעוד מפורט של איך כל פיצ'ר נבנה, שימושי לחיפוש/הבנה, אבל לא "המשימה הנוכחית"). ה"מצב סופי" האמיתי נמצא ב**ראש הקובץ הזה** וב-`NEXT_STEPS.md`.

1. **קרא `NEXT_STEPS.md` קודם** — הוא מסביר בדיוק לאן ממשיכים ומה לא להתחיל מיוזמתך (עיצוב/שירותים חיצוניים, שניהם דורשים קלט מהמשתמש שאין לך).
2. קרא את ראש הקובץ הזה (`PROJECT_STATE.md`, סעיף "מצב סופי — 2026-08-17") + `FEATURE_AUDIT.md` (במיוחד סעיף "Audit סופי — 2026-08-17" בתחתית) + `IMPLEMENTATION_GAPS.md`.
3. `git log --oneline -5` ו-`git status` בתוך `C:\Users\ארנון\OneDrive\Desktop\קלוד קוד\טיולים` — ודא שאין עבודה לא-שמורה.
4. **אל תתחיל Feature חדש, שלב עיצוב, או חיבור שירות חיצוני מיוזמתך.** נכון לתאריך זה, כל מה שאושר ולא-תלוי-בשירות-חיצוני הושלם. חכה להנחיה מפורשת מהמשתמש — ואם ההנחיה היא "תמשיך" בלי הקשר, שאל אם הכוונה לעיצוב או לחיבור שירות חיצוני (שניהם דורשים ממנו לספק משהו: חומרי עיצוב או מפתחות API).

## הערה טכנית חשובה על מנגנון ה-Context
אין לי כלי לבדוק את אחוז ניצול ה-Context שלי בזמן אמת בתוך השיחה, ואין לי יכולת להפעיל Compact באופן יזום בעצמי — אלה פעולות ברמת ה-harness (Claude Code), לא משהו שקוד/כלים שיש לי גישה אליהם יכולים לעשות. לכן אני שומר המשכיות לפי הטריגרים החלופיים שהוגדרו (אחרי כל רכיב משמעותי, לפני פעולות גדולות, כשיש חשד לסיכון) — לא לפי אחוז מדויק. אם המשתמש מריץ `/context` ורואה שהאחוז קרוב ל-80%, זה איתות טוב לבקש שמירה מפורשת אם היא לא כבר בוצעה לאחרונה.
